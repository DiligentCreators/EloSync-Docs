import{_ as t,o as d,c as o,a3 as a}from"./chunks/framework.BlfGB29P.js";const u=JSON.parse('{"title":"Leads — Developer Guide","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Leads — Developer Guide | EloSync Docs"}],["meta",{"property":"og:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"property":"og:url","content":"https://docs.elosync.com/developer-guide/leads"}],["meta",{"property":"og:image","content":"https://docs.elosync.com/og-image.svg"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:title","content":"Leads — Developer Guide | EloSync Docs"}],["meta",{"name":"twitter:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"name":"twitter:image","content":"https://docs.elosync.com/og-image.svg"}],["link",{"rel":"canonical","href":"https://docs.elosync.com/developer-guide/leads"}]]},"headers":[],"relativePath":"developer-guide/leads.md","filePath":"developer-guide/leads.md","lastUpdated":1786227487000}'),s={name:"developer-guide/leads.md"};function i(c,e,n,r,l,p){return d(),o("div",null,[...e[0]||(e[0]=[a(`<h1 id="leads-—-developer-guide" tabindex="-1">Leads — Developer Guide <a class="header-anchor" href="#leads-—-developer-guide" aria-label="Permalink to &quot;Leads — Developer Guide&quot;">​</a></h1><p>Reference implementation. Copy this layout for Tasks and later modules.</p><h2 id="backend-layout" tabindex="-1">Backend layout <a class="header-anchor" href="#backend-layout" aria-label="Permalink to &quot;Backend layout&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Models</td><td><code>app/Models/Lead.php</code>, <code>LeadStage</code>, <code>LeadTag</code>, <code>LeadNote</code>, <code>LeadNoteMention</code>, <code>LeadFollowUp</code>, <code>LeadActivity</code>, <code>LeadAssignmentHistory</code></td></tr><tr><td>Enums</td><td><code>app/Enums/Tenant/LeadStatusEnum</code>, <code>LeadPriorityEnum</code>, <code>LeadTypeEnum</code>, <code>LeadFollowUpStatusEnum</code>, <code>LeadActivityTypeEnum</code>, <code>LeadTagBehaviorEnum</code></td></tr><tr><td>Service</td><td><code>app/Services/Tenant/LeadService.php</code> (+ <code>ScopesToAssignee</code>), <code>LeadTagService.php</code></td></tr><tr><td>Export</td><td><code>app/Exports/LeadsExport.php</code></td></tr><tr><td>Import framework</td><td><code>app/Import/*</code> (<code>ImportManager</code>, <code>ImportFile</code>, <code>ImportColumnMapper</code>, <code>ImportErrorWriter</code>, <code>ImportTemplateGenerator</code>, <code>ImportHistory</code>, <code>ImportJob</code>)</td></tr><tr><td>Lead import handler</td><td><code>app/Import/Lead/LeadImportHandler.php</code>, <code>LeadImportValidator</code>, <code>LeadImportMapper</code></td></tr><tr><td>Import model</td><td><code>app/Models/LeadImport.php</code> (<code>lead_imports</code> table)</td></tr><tr><td>Import job</td><td><code>app/Jobs/ProcessLeadImportJob.php</code> → queue <code>imports</code></td></tr><tr><td>Controllers</td><td><code>LeadController.php</code>, <code>LeadImportController.php</code></td></tr><tr><td>Requests</td><td><code>app/Http/Requests/Tenant/Api/V1/Lead/*</code></td></tr><tr><td>Resources</td><td><code>app/Http/Resources/Tenant/Api/V1/Lead/*</code></td></tr><tr><td>Policy</td><td><code>app/Policies/LeadPolicy.php</code></td></tr><tr><td>Events</td><td><code>app/Events/Lead*.php</code></td></tr><tr><td>Subscriber</td><td><code>app/Listeners/LeadEventSubscriber.php</code> (audit + notifications)</td></tr><tr><td>Notifications</td><td><code>app/Notifications/Tenant/Lead/*</code> (assign: database+broadcast+webpush; follow-ups/mentions: database + optional mail; mentions also broadcast+webpush; inactivity: database+broadcast+webpush)</td></tr><tr><td>Inactivity job</td><td><code>app/Services/Tenant/LeadInactivityService.php</code>, <code>app/Console/Commands/NotifyInactiveLeadsCommand.php</code> (<code>leads:notify-inactive</code>, daily)</td></tr><tr><td>Mentions</td><td><code>App\\Support\\NoteMentions</code>, <code>NoteMentionService</code>; wired from <code>LeadNoteAdded</code> in <code>LeadEventSubscriber</code></td></tr><tr><td>Seeder</td><td><code>database/seeders/Tenant/LeadStageSeeder.php</code>, <code>LeadTagSeeder.php</code></td></tr><tr><td>Tests</td><td><code>tests/Feature/Tenant/Lead/LeadTest.php</code>, <code>LeadTagTest.php</code>, <code>LeadTypeTest.php</code>, <code>LeadValidationTest.php</code>, <code>LeadImportTest.php</code>, <code>LeadSameDayDuplicateTest.php</code>, <code>tests/Feature/Tenant/Notification/NoteMentionNotificationTest.php</code>, <code>tests/Unit/NoteMentionsTest.php</code></td></tr></tbody></table><h2 id="domain-notes" tabindex="-1">Domain notes <a class="header-anchor" href="#domain-notes" aria-label="Permalink to &quot;Domain notes&quot;">​</a></h2><ul><li>Disposition <strong>tags</strong> are many-to-many (<code>lead_lead_tag</code>), independent of stage/status. Catalog CRUD + reorder under <code>LeadTagController</code>. New leads receive <code>is_default</code> tags. Sync via <code>PUT /leads/{id}/tags</code> (and optional <code>tag_ids</code> on create). <code>auto_follow_up</code> creates a pending follow-up keyed by <code>lead_follow_ups.lead_tag_id</code>; <code>force_follow_up</code> requires a nested <code>follow_up</code> payload. System tag <code>duplicate</code> is seeded (protected from delete) and applied when email/phone matches another lead created the same workspace calendar day — manual create, import (Keep/Skip; Update only when a distinct same-day match remains after excluding the row being updated), and inbound ingest still notify assignee/creator/actor via <code>lead.duplicate_detected</code>.</li><li><strong>Lead type</strong> (<code>direct</code> | <code>company</code>) is stored on <code>leads.lead_type</code> (required on create; <code>sometimes|required</code> on update so partial PUTs may omit it; nullable for legacy rows). System tags <code>direct-lead</code> / <code>company-lead</code> are seeded in <code>LeadTagSeeder</code> and kept mutually exclusive via <code>LeadTagService::mergeExclusiveTypeTags()</code> on create, update, and manual tag sync.</li><li>Note bodies may include <code>@[Display Name](user:ID)</code> mention tokens (composer UI shows <code>@Name</code> chips). On <code>LeadNoteAdded</code>, <code>NoteMentionService</code> persists <code>lead_note_mentions</code> and sends <code>lead.mentioned</code> (skip self; idempotent via <code>dedupe_key</code>). Mail is optional via <code>email_notifications.lead_mentioned</code> (default off).</li><li>Follow-up <code>due_at</code> follows the <a href="/developer-guide/tenant-settings#timezone-and-scheduled-datetimes">Workspace timezone convention</a>: SPA edit/display in Settings → General timezone; store as UTC via <code>UtcDateTime</code> / <code>UtcIso</code>; due/overdue notifications use workspace-local “today”.</li><li><code>lead_value</code> replaced <code>estimated_value</code> (migration rename). Store/update requests still accept <code>estimated_value</code> as a write alias.</li><li>Status is independent of stage flags (<code>is_won</code> / <code>is_lost</code>). Stage change does not sync status.</li><li>Convert: <code>converted_at</code>, <code>conversion_meta</code>, status <code>closed</code>, activity type converted. When <a href="/developer-guide/contacts">Contacts</a> is entitled, creates/links a real <code>Contact</code> (<code>contact_id</code>, <code>conversion_meta.stub = false</code>, lifecycle <code>on_boarded</code>) and requires <code>contacts.create</code>; preserves the lead assignee; runs in a DB transaction. When <a href="/developer-guide/companies">Companies</a> is entitled and the lead has a non-empty <code>company</code> string, creates or reuses a Company by case-insensitive name (<code>conversion_meta.company_id</code>, Contact <code>company_id</code>) and requires <code>companies.create</code> only when creating. Company create is serialized with a per-tenant+name cache lock to reduce duplicates; a residual race without a unique DB name constraint remains (known limitation). Stale <code>conversion_meta.company_id</code> values are cleared when the company cannot be resolved. When <a href="/developer-guide/opportunities">Opportunities</a> is entitled and the request sets <code>create_opportunity: true</code> with <code>opportunity.name</code>, creates an Opportunity linked to the lead / contact / company (<code>conversion_meta.opportunity_id</code>) and requires <code>opportunities.create</code>. Stub converts (no <code>contact_id</code>) can be completed by calling convert again after Contacts is installed (company/opportunity already in <code>conversion_meta</code> are reused and linked; linking an existing opportunity requires <code>opportunities.update</code>). Without Contacts, conversion remains status-only for the contact (<code>conversion_meta.stub = true</code>) but may still create company/opportunity. <code>LeadPolicy::convert</code> uses the same assignee scope as update. Catalog version <strong>1.1.1</strong>.</li></ul><h3 id="known-limitations" tabindex="-1">Known limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known limitations&quot;">​</a></h3><ul><li><strong>Company name uniqueness on convert:</strong> Convert reuses companies by case-insensitive name and takes a short cache lock around create, but company names are not unique in the schema. Two concurrent converts of different leads with the same brand-new company name can still insert duplicates if the lock backend cannot coordinate (or races past the lock window). Operators should merge duplicates manually if that occurs; a future unique index would fully close this.- Assignee scoping via <code>ScopesToAssignee</code> with <code>leads.assign</code> (superadmin always org-wide).</li><li>Lead assignee eligibility (<code>User::eligibleLeadAssignees</code> / <code>EligibleLeadAssignee</code> rule): excludes suspended users, workspace owners (<code>superadmin</code>), and users with <code>exclude_from_lead_auto_assign</code>. Used by assign / create / update / bulk-assign / import column mapping, and by <code>LeadBulkAssignmentService::eligibleAssignees</code> for equal distribute.</li><li>Website webhook auto-assign uses <code>eligibleLeadAssignees ∩ receive_website_leads</code> when the endpoint flag <code>assign_to_website_recipients</code> is enabled. Meta Lead Ads does not use this pool.</li><li>Import equal distribute (<code>assignment_mode=equal</code>): requires <code>leads.assign</code> and that the importer is <code>manager_id</code> on ≥1 active department. Pool = eligible assignees ∩ members of those departments (<code>assignEquallyForImport</code>). Non-managers receive a validation error. Bulk equal distribute remains org-wide eligible assignees.</li><li><strong>Commission rate:</strong> <code>users.lead_commission_rate</code> (nullable decimal 0–100) is the user’s default. <code>LeadService::create</code> (when <code>assigned_to</code> is set) and <code>LeadService::update</code> / <code>assign</code> copy the assignee’s rate to <code>leads.commission_rate</code> on assign/reassign and clear it on unassign. Snapshot is reporting-only (export, list, detail) — no payout engine. Bulk assign and import equal distribute use <code>assign()</code> so snapshots apply there too.</li><li><strong>Inactivity alerts:</strong> Workspace setting <code>leads.inactivity_working_days</code> (integer, default <code>3</code>; <code>0</code> disables). Scheduled command <code>leads:notify-inactive</code> runs daily per tenant. Counts Mon–Sat working days in the workspace timezone (Sundays excluded). Idle = assigned lead in an open stage (not Won/Lost) with no meaningful <code>lead_activities</code> since the last assignment baseline. Meaningful types: <code>note_added</code>, <code>follow_up_created</code>, <code>follow_up_completed</code>, <code>stage_changed</code>, <code>status_changed</code>, <code>crm_activity_logged</code>, <code>crm_activity_completed</code>, <code>tags_changed</code>. Excluded from resetting idle: <code>assigned</code>, <code>reassigned</code>, <code>imported</code>, <code>created</code>. Notifies assignee (<code>lead.inactive</code>) plus department managers of the assignee (<code>lead.inactive_escalation</code>), else workspace owners. Idempotent via <code>NotificationIdempotency</code> (daily dedupe per lead/recipient).</li></ul><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><p><code>config/tenant-permissions.php</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>leads.view | create | update | delete | assign | export | import | convert</span></span></code></pre></div><p>Routes use <code>module:leads</code> then <code>can:leads.*</code> / policies.</p><h3 id="import-architecture" tabindex="-1">Import architecture <a class="header-anchor" href="#import-architecture" aria-label="Permalink to &quot;Import architecture&quot;">​</a></h3><ul><li>Reusable package: Maatwebsite Laravel Excel for CSV/XLSX read + templates</li><li>Entity-agnostic <code>app/Import</code> framework; Lead is the first handler (future modules add their own handlers)</li><li><code>LeadImportMapper</code> casts spreadsheet string fields (<code>phone</code>, <code>name</code>, <code>email</code>, <code>note</code>, etc.) to strings — Excel often returns phone as int/float</li><li>Every imported lead row uses <code>LeadService::create()</code> / <code>update()</code> — never bypasses business rules</li><li>Optional mapped <strong>Note</strong> column calls <code>LeadService::addNote()</code> after create (and after update when the cell is non-empty; empty cells are skipped)</li><li>All runs are async: <code>ProcessLeadImportJob::dispatch(...)-&gt;onQueue(&#39;imports&#39;)</code></li><li>Uploads stored on the configured uploads disk under <code>imports/{tenant_uuid}/</code></li><li>Single table <code>lead_imports</code> holds file metadata, mapping, options, status, stats, and report paths</li><li>Platform audit: <code>lead_import_completed</code> / <code>lead_import_failed</code></li></ul><p>Inbound ingestion (shipped): Custom Webhook + Meta Lead Ads use the <a href="/developer-guide/lead-source-driver-architecture">Lead Source Driver Architecture</a>. See <a href="/developer-guide/custom-lead-webhook">Custom Lead Webhook</a>, <a href="/developer-guide/meta-app-setup">Meta App Setup</a>, and <a href="/developer-guide/meta-lead-ads-integration">Meta Lead Ads</a>. Drivers normalize only; <code>LeadDuplicateService</code> + <code>LeadService</code> remain the sole write path.</p><h2 id="api-tenant" tabindex="-1">API (tenant) <a class="header-anchor" href="#api-tenant" aria-label="Permalink to &quot;API (tenant)&quot;">​</a></h2><p>Base: <code>/api/tenant/v1</code> — full reference <a href="/api/tenant-v1-leads">tenant-v1-leads.md</a>.</p><table tabindex="0"><thead><tr><th>Method</th><th>Path</th><th>Permission</th></tr></thead><tbody><tr><td>GET</td><td><code>/lead-stages</code></td><td>view</td></tr><tr><td>GET</td><td><code>/leads</code></td><td>view</td></tr><tr><td>GET</td><td><code>/leads/stats</code></td><td>view</td></tr><tr><td>GET</td><td><code>/leads/board</code></td><td>view</td></tr><tr><td>GET</td><td><code>/leads/export</code></td><td>export</td></tr><tr><td>GET</td><td><code>/leads/import/template</code></td><td>import</td></tr><tr><td>GET/POST</td><td><code>/leads/imports</code></td><td>import</td></tr><tr><td>GET/PUT</td><td><code>/leads/imports/{import}</code></td><td>import</td></tr><tr><td>PUT</td><td><code>/leads/imports/{import}/options</code></td><td>import</td></tr><tr><td>POST</td><td><code>/leads/imports/{import}/preview</code></td><td>import</td></tr><tr><td>POST</td><td><code>/leads/imports/{import}/run</code></td><td>import</td></tr><tr><td>GET</td><td><code>/leads/imports/{import}/file</code></td><td>import</td></tr><tr><td>GET</td><td><code>/leads/imports/{import}/failed-records</code></td><td>import</td></tr><tr><td>GET</td><td><code>/leads/imports/{import}/error-report</code></td><td>import</td></tr><tr><td>POST</td><td><code>/leads</code></td><td>create</td></tr><tr><td>GET</td><td><code>/leads/{lead}</code></td><td>view</td></tr><tr><td>PUT</td><td><code>/leads/{lead}</code></td><td>update</td></tr><tr><td>DELETE</td><td><code>/leads/{lead}</code></td><td>delete</td></tr><tr><td>POST</td><td><code>/leads/{lead}/assign</code></td><td>assign</td></tr><tr><td>POST</td><td><code>/leads/{lead}/convert</code></td><td>convert</td></tr><tr><td>POST</td><td><code>/leads/{lead}/stage</code></td><td>update</td></tr><tr><td>POST</td><td><code>/leads/{lead}/notes</code></td><td>update</td></tr><tr><td>POST</td><td><code>/leads/{lead}/follow-ups</code></td><td>update</td></tr><tr><td>PUT</td><td><code>/leads/{lead}/follow-ups/{followUp}</code></td><td>update</td></tr><tr><td>POST</td><td><code>/leads/{lead}/follow-ups/{followUp}/complete</code></td><td>update</td></tr><tr><td>GET</td><td><code>/leads/{lead}/timeline</code></td><td>view</td></tr><tr><td>GET</td><td><code>/leads/{lead}/assignment-history</code></td><td>view</td></tr></tbody></table><p>Auth login/<code>me</code> include <code>modules: string[]</code> for SPA gating.</p><h2 id="frontend" tabindex="-1">Frontend <a class="header-anchor" href="#frontend" aria-label="Permalink to &quot;Frontend&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Page</td><td><code>src/pages/leads/leads-page.tsx</code> (board default + table)</td></tr><tr><td>Form</td><td><code>lead-form-dialog.tsx</code></td></tr><tr><td>Detail</td><td><code>lead-detail-sheet.tsx</code> (Notes use <code>MentionComposer</code>; board DnD auto-saves stage on the list page)</td></tr><tr><td>Import wizard</td><td><code>lead-import-dialog.tsx</code> (5-step)</td></tr><tr><td>Import history</td><td><code>lead-import-history-dialog.tsx</code></td></tr><tr><td>Shared board</td><td><code>src/components/crm/kanban-board.tsx</code></td></tr><tr><td>Mentions UI</td><td><code>src/components/crm/mention-composer.tsx</code> (shows <code>@Name</code> chips; emits <code>@[Name](user:id)</code>; keep typing after pick; Backspace/Delete removes chips), <code>src/lib/note-mentions.ts</code> (<code>formatNoteMentionsForDisplay</code> in detail sheets + <code>latest-note-follow-up.tsx</code> list/board previews)</td></tr><tr><td>Notification registry</td><td><code>src/notifications/modules/crm.ts</code> (<code>lead.mentioned</code>, <code>lead.duplicate_detected</code>, <code>lead.inactive</code>, <code>lead.inactive_escalation</code>)</td></tr><tr><td>Service</td><td><code>leadService</code> in <code>src/api/services.ts</code></td></tr><tr><td>Nav</td><td><code>permission: leads.view</code>, <code>module: &#39;leads&#39;</code></td></tr></tbody></table><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Backend</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/Lead</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/Lead/LeadImportTest.php</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Worker (import jobs)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> queue:work</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --queue=imports,default</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Frontend E2E</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e:leads</span></span></code></pre></div><h2 id="logging" tabindex="-1">Logging <a class="header-anchor" href="#logging" aria-label="Permalink to &quot;Logging&quot;">​</a></h2><ul><li>Spatie <code>LogsActivity</code> on <code>Lead</code> (log name <code>leads</code>)</li><li>Domain <code>lead_activities</code> timeline</li><li><code>lead_assignment_histories</code> for assignee changes</li><li><code>users.lead_commission_rate</code>, <code>leads.commission_rate</code> — snapshot on assign via <code>LeadService::assign</code></li><li><code>PlatformAuditService</code> via <code>LeadEventSubscriber</code> (+ <code>lead_import_completed</code> / <code>lead_import_failed</code>)</li></ul>`,25)])])}const h=t(s,[["render",i]]);export{u as __pageData,h as default};
