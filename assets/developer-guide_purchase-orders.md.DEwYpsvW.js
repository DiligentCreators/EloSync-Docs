import{_ as t,o as d,c as o,a3 as r}from"./chunks/framework.BlfGB29P.js";const u=JSON.parse('{"title":"Purchase Orders — Developer Guide","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Purchase Orders — Developer Guide | EloSync Docs"}],["meta",{"property":"og:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"property":"og:url","content":"https://docs.elosync.com/developer-guide/purchase-orders"}],["meta",{"property":"og:image","content":"https://docs.elosync.com/og-image.svg"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:title","content":"Purchase Orders — Developer Guide | EloSync Docs"}],["meta",{"name":"twitter:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"name":"twitter:image","content":"https://docs.elosync.com/og-image.svg"}],["link",{"rel":"canonical","href":"https://docs.elosync.com/developer-guide/purchase-orders"}]]},"headers":[],"relativePath":"developer-guide/purchase-orders.md","filePath":"developer-guide/purchase-orders.md","lastUpdated":1786835950000}'),s={name:"developer-guide/purchase-orders.md"};function a(c,e,i,n,l,p){return d(),o("div",null,[...e[0]||(e[0]=[r(`<h1 id="purchase-orders-—-developer-guide" tabindex="-1">Purchase Orders — Developer Guide <a class="header-anchor" href="#purchase-orders-—-developer-guide" aria-label="Permalink to &quot;Purchase Orders — Developer Guide&quot;">​</a></h1><p>Mirror of the <a href="/developer-guide/estimates">Estimates developer guide</a> (assignee scope, notes, domain timeline, hard module dependency, first-class <code>lines</code> child table, status machine) and the optional related-record pickers — swapped for a single <strong>required</strong> <code>vendor_id</code>. A <strong>soft</strong> convert-to-expense action was added in Phase 4 Milestone 3 (see <a href="/developer-guide/expenses">Expenses developer guide</a>). Prefer copying those patterns over inventing new ones.</p><h2 id="backend-layout" tabindex="-1">Backend layout <a class="header-anchor" href="#backend-layout" aria-label="Permalink to &quot;Backend layout&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Models</td><td><code>app/Models/PurchaseOrder.php</code>, <code>PurchaseOrderLine</code>, <code>PurchaseOrderNote</code>, <code>PurchaseOrderActivity</code></td></tr><tr><td>Enums</td><td><code>PurchaseOrderStatusEnum</code>, <code>PurchaseOrderActivityTypeEnum</code></td></tr><tr><td>Service</td><td><code>app/Services/Tenant/PurchaseOrderService.php</code> (+ <code>ScopesToAssignee</code>, <code>RetriesOnDuplicateNumber</code>)</td></tr><tr><td>Controller</td><td><code>app/Http/Controllers/Tenant/Api/V1/PurchaseOrderController.php</code></td></tr><tr><td>Requests</td><td><code>app/Http/Requests/Tenant/Api/V1/PurchaseOrder/*</code></td></tr><tr><td>Resources</td><td><code>app/Http/Resources/Tenant/Api/V1/PurchaseOrder/*</code></td></tr><tr><td>Policy</td><td><code>app/Policies/PurchaseOrderPolicy.php</code></td></tr><tr><td>Events</td><td><code>app/Events/PurchaseOrder*.php</code></td></tr><tr><td>Subscriber</td><td><code>app/Listeners/PurchaseOrderEventSubscriber.php</code> (audit + assignment notification)</td></tr><tr><td>Notifications</td><td><code>app/Notifications/Tenant/PurchaseOrder/PurchaseOrderAssignedNotification.php</code></td></tr><tr><td>Link rule</td><td><code>app/Rules/LinkableVendor.php</code> — <code>vendor_id</code> is required, tenant-scoped, and validates the Vendors module is entitled</td></tr><tr><td>Dependency migration</td><td><code>database/migrations/2026_08_01_120006_add_purchase_orders_vendors_dependency.php</code> (mirrors estimates → invoices)</td></tr><tr><td>Convert</td><td><code>PurchaseOrderService::convertToExpense()</code> (soft Expenses entitlement check, no hard dependency) — see <a href="/developer-guide/expenses#convert-from-purchase-order-soft">Expenses developer guide</a></td></tr><tr><td>Convert permission migration</td><td><code>database/migrations/2026_08_01_130005_add_purchase_orders_convert_permission.php</code></td></tr><tr><td>Factories</td><td><code>PurchaseOrderFactory</code>, <code>PurchaseOrderLineFactory</code>, <code>PurchaseOrderNoteFactory</code>, <code>PurchaseOrderActivityFactory</code></td></tr><tr><td>Tests</td><td><code>tests/Feature/Tenant/PurchaseOrder/PurchaseOrderTest.php</code>, <code>tests/Feature/Central/Module/PurchaseOrdersModuleDependencyTest.php</code></td></tr></tbody></table><h2 id="domain-notes" tabindex="-1">Domain notes <a class="header-anchor" href="#domain-notes" aria-label="Permalink to &quot;Domain notes&quot;">​</a></h2><ul><li><strong>Hard dependency</strong>: Purchase Orders declares a required <code>module_dependencies</code> row on Vendors — Marketplace install is blocked until Vendors is entitled, same pattern as Estimates → Invoices.</li><li>Status machine lives on <code>PurchaseOrderStatusEnum::allowedTransitions()</code> / <code>canTransitionTo()</code>: <code>draft → sent|cancelled</code>, <code>sent → partially_received|received|cancelled</code>, <code>partially_received → received|cancelled</code>, <code>received</code>/<code>cancelled</code> are terminal. <code>PurchaseOrderService::transitionStatus()</code> throws <code>ValidationException</code> (422, <code>status</code> field) for disallowed transitions.</li><li><code>send()</code> backfills <code>order_date</code> to today if it wasn&#39;t already set, then transitions <code>draft → sent</code>.</li><li><code>receive()</code> only accepts a target status of <code>partially_received</code> or <code>received</code> — any other value throws a 422 validation error before even checking the state machine.</li><li>Content updates (<code>PUT</code>) and line sync are <strong>draft-only</strong> via <code>PurchaseOrder::isEditable()</code> (<code>status === draft</code>). Assignment remains available after send via <code>POST …/assign</code>.</li><li><code>POST …/status</code> route middleware requires <code>purchase-orders.update</code>; the controller then re-checks the specific gate per target status (<code>sent</code> → <code>send</code>, <code>partially_received</code>/<code>received</code> → <code>receive</code>, <code>cancelled</code> → <code>cancel</code>, otherwise <code>update</code>) before delegating to <code>PurchaseOrderService::changeStatus()</code>. Receive statuses (<code>partially_received</code> / <code>received</code>) are routed through <code>receive()</code> so <code>/status</code> and <code>/receive</code> share the same inventory posting and atomicity guarantees (mirrors invoices routing <code>void</code> through <code>void()</code>).</li><li><code>send</code> / <code>receive</code> / <code>cancel</code> policies are assignee-scoped (same as <code>view</code> / <code>update</code>) unless the actor has <code>purchase-orders.assign</code> or is superadmin.</li><li>Lines are a first-class child table (<code>purchase_order_lines</code>), not embedded JSON — each row is <code>{ description, quantity, unit_price, tax_rate, sort_order, product_id? }</code>. <code>product_id</code> is nullable and, when supplied, is tenant/entitlement validated by <code>LinkableProduct</code>. <code>subtotal</code>/<code>tax_total</code>/<code>total</code> are recomputed server-side from lines on create/update, same as Estimates/Invoices/Quotations.</li><li>Assignee scoping via <code>ScopesToAssignee</code> with <code>purchase-orders.assign</code>.</li><li><code>purchase-orders.force.delete</code> is not granted to any default role — owner/superadmin only.</li><li><code>vendor_id</code> is <strong>required</strong> (unlike Estimates&#39; optional contact/company) and validated via <code>LinkableVendor</code> — must exist, belong to the tenant, and the Vendors module must be entitled.</li><li>Auto-numbering: <code>PurchaseOrderService::nextNumber()</code> reads the <code>purchase_orders_number_prefix</code> tenant setting (default <code>PO-</code>), then zero-pads a running count to 5 digits — same pattern as Estimates/Invoices/Payments. Exposed via <code>PUT /settings</code> (<code>UpdateTenantSettingsRequest</code>), not yet surfaced in the Tenant Settings UI. <code>purchase_orders</code> has a <code>unique(tenant_id, number)</code> DB index; <code>create()</code> retries up to 3 times via the shared <code>RetriesOnDuplicateNumber</code> trait on a duplicate-key collision.</li><li><strong>Receiving bridge</strong> — <code>partially_received</code> remains acknowledgement-only. On <code>received</code>, when Products and Inventory are entitled, <code>StockService::postPurchaseOrderReceipt()</code> posts stock-in once for linked <code>track_stock</code> product lines, using the optional receive <code>warehouse_id</code> or the default warehouse. <code>receive()</code> runs status transition + stock post in one DB transaction with <code>lockForUpdate()</code> on the purchase order (and again inside receipt posting) so concurrent receives cannot double-post and a failed stock post rolls the status back.</li><li><strong>Convert to expense is soft, one-way, one-time</strong>: <code>PurchaseOrderService::convertToExpense()</code> checks <code>EntitlementService::hasModule($tenant, &#39;expenses&#39;)</code> at call time (not a hard <code>module_dependencies</code> row), rejects if an <code>Expense</code> already references this <code>purchase_order_id</code> (<code>withTrashed()</code> check), and only allows <code>sent</code>/<code>partially_received</code>/<code>received</code> source statuses via <code>PurchaseOrder::isConvertible()</code>. <code>PurchaseOrder::convertedExpense()</code> (<code>hasOne</code>) and <code>ListPurchaseOrderResource.converted_expense_id</code> let the frontend hide the action once used.</li></ul><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>purchase-orders.view | create | update | delete | restore | force.delete | assign | send | receive | cancel | convert</span></span></code></pre></div><p>Routes use <code>module:purchase-orders</code> then <code>can:purchase-orders.*</code> / policies.</p><p>Catalog: slug <code>purchase-orders</code>, category <code>purchasing</code>, <code>is_default_included = false</code>, <code>is_billable = false</code>, <code>sort_order = 20</code>. Registered via <code>DefaultModuleRegistrar</code> migration (migrate-only), with a follow-up migration inserting the <code>module_dependencies</code> row on <code>vendors</code>.</p><h2 id="api-tenant" tabindex="-1">API (tenant) <a class="header-anchor" href="#api-tenant" aria-label="Permalink to &quot;API (tenant)&quot;">​</a></h2><p>Base: <code>/api/tenant/v1</code> — full reference <a href="/api/tenant-v1-purchase-orders">tenant-v1-purchase-orders.md</a>.</p><h2 id="frontend" tabindex="-1">Frontend <a class="header-anchor" href="#frontend" aria-label="Permalink to &quot;Frontend&quot;">​</a></h2><p>SPA mirrors <strong>Estimates</strong> (table + form dialog, detail sheet) under the existing AppLayout — do not invent a parallel shell.</p><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Page</td><td><code>src/pages/purchase-orders/</code> (<code>purchase-orders-page.tsx</code>, <code>purchase-order-form-dialog.tsx</code>, <code>purchase-order-detail-sheet.tsx</code>)</td></tr><tr><td>Detail sheet</td><td>Overview (vendor, totals, dates, assignee, related converted expense), line items, notes, timeline — actions: assign, add note, send, mark partially received, mark received, cancel, convert to expense (soft), edit (draft only), delete</td></tr><tr><td>Form dialog</td><td>Title, required vendor picker (<code>SearchableSelect</code> backed by <code>vendorService.list()</code>), currency, order date, expected date, notes, and a line-items editor (<code>useFieldArray</code>) with live subtotal/tax/total preview</td></tr><tr><td>Service</td><td><code>purchaseOrderService</code> in <code>src/api/services.ts</code></td></tr><tr><td>Types</td><td><code>PurchaseOrder*</code> in <code>src/types/api.ts</code></td></tr><tr><td>Query keys</td><td><code>QUERY_KEYS.purchaseOrders</code> / <code>purchaseOrder(id)</code> / <code>purchaseOrderTimeline(id)</code> / <code>purchaseOrderStats</code></td></tr><tr><td>Permissions</td><td><code>PERMISSIONS.purchaseOrders.*</code> (maps to <code>purchase-orders.*</code> permission strings)</td></tr><tr><td>Nav</td><td><strong>Purchasing</strong> sidebar group, after Vendors — <code>permission: PERMISSIONS.purchaseOrders.view</code>, <code>module: &#39;purchase-orders&#39;</code></td></tr><tr><td>Route</td><td><code>tenantRoutes.purchaseOrders = &#39;/purchase-orders&#39;</code>, lazy-loaded in <code>App.tsx</code> behind <code>RequireAccess module=&quot;purchase-orders&quot;</code></td></tr><tr><td>Notifications</td><td><code>src/notifications/modules/purchase-orders.ts</code> — <code>purchase-order.assigned</code> → <code>/purchase-orders?purchase_order={id}</code></td></tr><tr><td>Playwright</td><td><code>e2e/pages/purchase-orders.page.ts</code>, <code>e2e/tests/purchase-orders/</code>, <code>npm run test:e2e:purchase-orders</code></td></tr></tbody></table><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/PurchaseOrder/PurchaseOrderTest.php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Central/Module/PurchaseOrdersModuleDependencyTest.php</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e:purchase-orders</span></span></code></pre></div><h2 id="logging" tabindex="-1">Logging <a class="header-anchor" href="#logging" aria-label="Permalink to &quot;Logging&quot;">​</a></h2><ul><li>Spatie <code>LogsActivity</code> on <code>PurchaseOrder</code> (log name <code>purchase-orders</code>)</li><li>Domain <code>purchase_order_activities</code> timeline</li><li><code>PlatformAuditService</code> via <code>PurchaseOrderEventSubscriber</code></li></ul><h2 id="intentional-differences-from-estimates" tabindex="-1">Intentional differences from Estimates <a class="header-anchor" href="#intentional-differences-from-estimates" aria-label="Permalink to &quot;Intentional differences from Estimates&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Estimates</th><th>Purchase Orders</th></tr></thead><tbody><tr><td>Optional <code>contact_id</code>/<code>company_id</code>/<code>opportunity_id</code>/<code>quotation_id</code></td><td>Single <strong>required</strong> <code>vendor_id</code></td></tr><tr><td>Hard-depends on Invoices</td><td>Hard-depends on <strong>Vendors</strong></td></tr><tr><td><code>convert()</code> → draft <code>CustomerInvoice</code> (hard dependency)</td><td><code>convertToExpense()</code> → draft <code>Expense</code> (<strong>soft</strong> entitlement check, no hard dependency)</td></tr><tr><td>Statuses: <code>draft → sent → accepted|rejected|expired</code></td><td>Statuses: <code>draft → sent → partially_received|received|cancelled</code></td></tr><tr><td><code>accept</code> / <code>send</code> / <code>convert</code> actions</td><td><code>send</code> / <code>receive</code> / <code>cancel</code> / <code>convert</code> actions</td></tr></tbody></table><h2 id="deferred" tabindex="-1">Deferred <a class="header-anchor" href="#deferred" aria-label="Permalink to &quot;Deferred&quot;">​</a></h2><ul><li>Per-line partial receiving</li><li>Dashboard widgets for Purchase Orders</li><li>Communication template placeholders for Purchase Orders</li></ul>`,23)])])}const m=t(s,[["render",a]]);export{u as __pageData,m as default};
