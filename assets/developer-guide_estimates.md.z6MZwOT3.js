import{_ as t,o,c as i,a3 as s}from"./chunks/framework.BlfGB29P.js";const h=JSON.parse('{"title":"Estimates — Developer Guide","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Estimates — Developer Guide | EloSync Docs"}],["meta",{"property":"og:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"property":"og:url","content":"https://docs.elosync.com/developer-guide/estimates"}],["meta",{"property":"og:image","content":"https://docs.elosync.com/og-image.svg"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:title","content":"Estimates — Developer Guide | EloSync Docs"}],["meta",{"name":"twitter:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"name":"twitter:image","content":"https://docs.elosync.com/og-image.svg"}],["link",{"rel":"canonical","href":"https://docs.elosync.com/developer-guide/estimates"}]]},"headers":[],"relativePath":"developer-guide/estimates.md","filePath":"developer-guide/estimates.md","lastUpdated":1786227487000}'),d={name:"developer-guide/estimates.md"};function a(c,e,n,r,l,p){return o(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="estimates-—-developer-guide" tabindex="-1">Estimates — Developer Guide <a class="header-anchor" href="#estimates-—-developer-guide" aria-label="Permalink to &quot;Estimates — Developer Guide&quot;">​</a></h1><p>Mirror of the <a href="/developer-guide/credit-notes">Credit Notes developer guide</a> (assignee scope, notes, domain timeline, hard dependency on Invoices, first-class <code>lines</code> child table), plus a <strong>convert-to-invoice</strong> action modeled after Quotations&#39; status machine. Prefer copying those patterns over inventing new ones.</p><h2 id="backend-layout" tabindex="-1">Backend layout <a class="header-anchor" href="#backend-layout" aria-label="Permalink to &quot;Backend layout&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Models</td><td><code>app/Models/Estimate.php</code>, <code>EstimateLine</code>, <code>EstimateNote</code>, <code>EstimateActivity</code></td></tr><tr><td>Enums</td><td><code>EstimateStatusEnum</code>, <code>EstimateActivityTypeEnum</code></td></tr><tr><td>Service</td><td><code>app/Services/Tenant/EstimateService.php</code> (+ <code>ScopesToAssignee</code>, injects <code>CustomerInvoiceService</code> for conversion)</td></tr><tr><td>Controller</td><td><code>app/Http/Controllers/Tenant/Api/V1/EstimateController.php</code></td></tr><tr><td>Requests</td><td><code>app/Http/Requests/Tenant/Api/V1/Estimate/*</code></td></tr><tr><td>Resources</td><td><code>app/Http/Resources/Tenant/Api/V1/Estimate/*</code></td></tr><tr><td>Policy</td><td><code>app/Policies/EstimatePolicy.php</code></td></tr><tr><td>Events</td><td><code>app/Events/Estimate*.php</code> (including <code>EstimateConverted</code>)</td></tr><tr><td>Subscriber</td><td><code>app/Listeners/EstimateEventSubscriber.php</code> (audit + assignment notification)</td></tr><tr><td>Notifications</td><td><code>app/Notifications/Tenant/Estimate/EstimateAssignedNotification.php</code></td></tr><tr><td>Link rules</td><td><code>LinkableContact</code>, <code>LinkableCompany</code> — <code>quotation_id</code>/<code>opportunity_id</code> are plain tenant-scoped <code>Rule::exists()</code> on <code>Quotation</code>/<code>Opportunity</code></td></tr><tr><td>Dependency migration</td><td><code>database/migrations/2026_07_31_223006_add_estimates_invoices_dependency.php</code> (mirrors credit-notes → invoices)</td></tr><tr><td>Cross-module FK</td><td><code>database/migrations/2026_07_31_223007_add_estimate_foreign_key_to_customer_invoices_table.php</code> adds the <code>customer_invoices.estimate_id</code> foreign key (the column itself already existed nullable from the Invoices migration)</td></tr><tr><td>Factories</td><td><code>EstimateFactory</code>, <code>EstimateLineFactory</code>, <code>EstimateNoteFactory</code>, <code>EstimateActivityFactory</code></td></tr><tr><td>Tests</td><td><code>tests/Feature/Tenant/Estimate/EstimateTest.php</code>, <code>tests/Feature/Central/Module/EstimatesModuleDependencyTest.php</code></td></tr></tbody></table><h2 id="domain-notes" tabindex="-1">Domain notes <a class="header-anchor" href="#domain-notes" aria-label="Permalink to &quot;Domain notes&quot;">​</a></h2><ul><li><strong>Hard dependency</strong>: Estimates declares a required <code>module_dependencies</code> row on Invoices — Marketplace install is blocked until Invoices is entitled, same pattern as Payments/Credit Notes → Invoices.</li><li>Status machine lives on <code>EstimateStatusEnum::allowedTransitions()</code> / <code>canTransitionTo()</code>: <code>draft → sent → accepted|rejected|expired</code> (identical shape to <code>QuotationStatusEnum</code>). <code>EstimateService::transitionStatus()</code> throws <code>ValidationException</code> (422, <code>status</code> field) for disallowed transitions, <strong>including re-entering the same status</strong>.</li><li><code>send()</code> backfills <code>issue_date</code> to today if it wasn&#39;t already set, then transitions <code>draft → sent</code>.</li><li>Content updates (<code>PUT</code>) and line sync are <strong>draft-only</strong> via <code>Estimate::isEditable()</code> (<code>status === draft</code>). Assignment remains available after send via <code>POST …/assign</code>.</li><li><code>POST …/status</code> route middleware requires <code>estimates.update</code>; the controller then re-checks the specific gate per target status (<code>sent</code> → <code>send</code>, <code>accepted</code> → <code>accept</code>, otherwise <code>update</code>) before delegating to <code>EstimateService::changeStatus()</code>.</li><li><code>send</code> / <code>accept</code> / <code>convert</code> policies are assignee-scoped (same as <code>view</code> / <code>update</code>) unless the actor has <code>estimates.assign</code> or is superadmin.</li><li><strong><code>convertToInvoice()</code></strong> (<code>EstimateService</code>): <ol><li>Rejects if a <code>CustomerInvoice</code> already has this <code>estimate_id</code> (including soft-deleted ones) — an estimate converts <strong>at most once</strong>.</li><li>Rejects unless <code>Estimate::isConvertible()</code> (<code>status</code> is <code>sent</code> or <code>accepted</code>).</li><li>Inside a DB transaction: copies <code>title</code>, <code>notes</code>, <code>currency</code>, <code>contact_id</code>, <code>company_id</code>, <code>quotation_id</code>, <code>assigned_to</code>, and a mapped copy of every <code>EstimateLine</code> into <code>CustomerInvoiceService::create()</code>, producing a <strong>draft</strong> invoice; sets <code>invoice-&gt;estimate_id</code> and saves.</li><li>Transitions the estimate to <code>accepted</code> if it wasn&#39;t already (reuses <code>transitionStatus()</code>).</li><li>Records a <code>converted</code> activity on the estimate and fires <code>EstimateConverted($estimate, $invoice, $actor)</code>.</li><li>Returns the created <code>CustomerInvoice</code> (loaded with its own relations) — the controller renders it via <code>CustomerInvoiceResource</code>, not an Estimate resource.</li></ol></li><li>Lines are a first-class child table (<code>estimate_lines</code>), not embedded JSON — each row is <code>{ description, quantity, unit_price, tax_rate, sort_order }</code>. <code>subtotal</code>/<code>tax_total</code>/<code>total</code> are recomputed server-side from lines on create/update, same as Invoices/Quotations/Credit Notes.</li><li>Assignee scoping via <code>ScopesToAssignee</code> with <code>estimates.assign</code>.</li><li><code>estimates.force.delete</code> is not granted to any default role — owner/superadmin only.</li><li><code>contact_id</code> / <code>company_id</code> are optional and validated for module entitlement + assignee scope (<code>LinkableContact</code> / <code>LinkableCompany</code>). <code>opportunity_id</code> / <code>quotation_id</code> are optional, tenant-scoped <code>exists()</code> checks (a quotation belongs to an opportunity, but the estimate doesn&#39;t enforce that the two match).</li><li>Auto-numbering: <code>EstimateService::nextNumber()</code> reads the <code>estimates_number_prefix</code> tenant setting (default <code>EST-</code>), then zero-pads a running count to 5 digits — same pattern as Invoices/Payments/Credit Notes. Not yet exposed in the Tenant Settings UI. <code>estimates</code> has a <code>unique(tenant_id, number)</code> DB index; <code>create()</code> retries up to 3 times via the shared <code>RetriesOnDuplicateNumber</code> trait on a duplicate-key collision.</li></ul><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>estimates.view | create | update | delete | restore | force.delete | assign | send | accept | convert</span></span></code></pre></div><p>Routes use <code>module:estimates</code> then <code>can:estimates.*</code> / policies.</p><p>Catalog: slug <code>estimates</code>, category <code>billing</code>, <code>is_default_included = false</code>, <code>is_billable = false</code>, <code>sort_order = 40</code>. Registered via <code>DefaultModuleRegistrar</code> migration (migrate-only), with a follow-up migration inserting the <code>module_dependencies</code> row on <code>invoices</code>.</p><h2 id="api-tenant" tabindex="-1">API (tenant) <a class="header-anchor" href="#api-tenant" aria-label="Permalink to &quot;API (tenant)&quot;">​</a></h2><p>Base: <code>/api/tenant/v1</code> — full reference <a href="/api/tenant-v1-estimates">tenant-v1-estimates.md</a>.</p><h2 id="frontend" tabindex="-1">Frontend <a class="header-anchor" href="#frontend" aria-label="Permalink to &quot;Frontend&quot;">​</a></h2><p>SPA mirrors <strong>Invoices</strong>/<strong>Quotations</strong> (table + form dialog, detail sheet) under the existing AppLayout — do not invent a parallel shell.</p><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Page</td><td><code>src/pages/estimates/</code> (<code>estimates-page.tsx</code>, <code>estimate-form-dialog.tsx</code>, <code>estimate-detail-sheet.tsx</code>)</td></tr><tr><td>Detail sheet</td><td>Overview, linked contact/company/opportunity/quotation/converted invoice, line items, notes, timeline — actions: assign, add note, send, accept, reject, <strong>convert to invoice</strong>, edit (draft only), delete</td></tr><tr><td>Form dialog</td><td>Title, currency, valid-until, notes, contact/company pickers, opportunity picker (quotation picker filtered by the selected opportunity), and a line-items editor (<code>useFieldArray</code>) with live subtotal/tax/total preview</td></tr><tr><td>Service</td><td><code>estimateService</code> in <code>src/api/services.ts</code></td></tr><tr><td>Types</td><td><code>Estimate*</code> in <code>src/types/api.ts</code></td></tr><tr><td>Query keys</td><td><code>QUERY_KEYS.estimates</code> / <code>estimate(id)</code> / <code>estimateTimeline(id)</code> / <code>estimateStats</code></td></tr><tr><td>Permissions</td><td><code>PERMISSIONS.estimates.*</code> (maps to <code>estimates.*</code> permission strings)</td></tr><tr><td>Nav</td><td><strong>Billing</strong> sidebar group, after Credit Notes — <code>permission: PERMISSIONS.estimates.view</code>, <code>module: &#39;estimates&#39;</code></td></tr><tr><td>Route</td><td><code>tenantRoutes.estimates = &#39;/estimates&#39;</code>, lazy-loaded in <code>App.tsx</code> behind <code>RequireAccess module=&quot;estimates&quot;</code></td></tr><tr><td>Notifications</td><td><code>src/notifications/modules/estimates.ts</code> — <code>estimate.assigned</code> → <code>/estimates?estimate={id}</code></td></tr><tr><td>Playwright</td><td><code>e2e/pages/estimates.page.ts</code>, <code>e2e/tests/estimates/</code>, <code>npm run test:e2e:estimates</code></td></tr></tbody></table><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/Estimate/EstimateTest.php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Central/Module/EstimatesModuleDependencyTest.php</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e:estimates</span></span></code></pre></div><h2 id="logging" tabindex="-1">Logging <a class="header-anchor" href="#logging" aria-label="Permalink to &quot;Logging&quot;">​</a></h2><ul><li>Spatie <code>LogsActivity</code> on <code>Estimate</code> (log name <code>estimates</code>)</li><li>Domain <code>estimate_activities</code> timeline</li><li><code>PlatformAuditService</code> via <code>EstimateEventSubscriber</code></li></ul>`,19)])])}const u=t(d,[["render",a]]);export{h as __pageData,u as default};
