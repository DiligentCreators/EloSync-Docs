import{_ as o,o as t,c as d,a3 as i}from"./chunks/framework.BTQ4eCVb.js";const h=JSON.parse('{"title":"Invoices — Developer Guide","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Invoices — Developer Guide | EloSync Docs"}],["meta",{"property":"og:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"property":"og:url","content":"https://docs.elosync.com/developer-guide/invoices"}],["meta",{"property":"og:image","content":"https://docs.elosync.com/og-image.svg"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:title","content":"Invoices — Developer Guide | EloSync Docs"}],["meta",{"name":"twitter:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"name":"twitter:image","content":"https://docs.elosync.com/og-image.svg"}],["link",{"rel":"canonical","href":"https://docs.elosync.com/developer-guide/invoices"}]]},"headers":[],"relativePath":"developer-guide/invoices.md","filePath":"developer-guide/invoices.md","lastUpdated":1788123957000}'),c={name:"developer-guide/invoices.md"};function n(s,e,a,r,l,u){return t(),d("div",null,[...e[0]||(e[0]=[i(`<h1 id="invoices-—-developer-guide" tabindex="-1">Invoices — Developer Guide <a class="header-anchor" href="#invoices-—-developer-guide" aria-label="Permalink to &quot;Invoices — Developer Guide&quot;">​</a></h1><p>Mirror of the <a href="/developer-guide/quotations">Quotations developer guide</a> (assignee scope, notes, domain timeline, line items), kept module-standalone: <strong>no required FK / hard dependency</strong>, unlike Quotations/Contracts which require Opportunities.</p><blockquote><p><strong>Naming:</strong> the backend model is <code>CustomerInvoice</code> (table <code>customer_invoices</code>) — Central&#39;s own platform-billing <code>Invoice</code> model already exists for subscription invoices the platform sends <em>to</em> tenants. Frontend mirrors this with <code>customerInvoiceService</code> / <code>PERMISSIONS.customerInvoices</code> / <code>QUERY_KEYS.customerInvoices</code>, distinct from the pre-existing <code>invoiceService</code> / <code>PERMISSIONS.invoices</code>.</p></blockquote><h2 id="backend-layout" tabindex="-1">Backend layout <a class="header-anchor" href="#backend-layout" aria-label="Permalink to &quot;Backend layout&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Models</td><td><code>app/Models/CustomerInvoice.php</code>, <code>CustomerInvoiceLine</code>, <code>CustomerInvoiceNote</code>, <code>CustomerInvoiceActivity</code></td></tr><tr><td>Enums</td><td><code>CustomerInvoiceStatusEnum</code>, <code>CustomerInvoiceActivityTypeEnum</code>, <code>CustomerInvoiceRecurrenceFrequencyEnum</code>, <code>CustomerInvoiceRecurrenceStatusEnum</code>, <code>DocumentDiscountTypeEnum</code></td></tr><tr><td>Support</td><td><code>app/Support/Billing/DocumentTotalsCalculator.php</code>, <code>DocumentDiscountRules.php</code>, <code>DocumentHtmlSanitizer.php</code>, <code>BrandedDocumentPdfContext.php</code></td></tr><tr><td>Service</td><td><code>app/Services/Tenant/CustomerInvoiceService.php</code> (+ <code>ScopesToAssignee</code>)</td></tr><tr><td>PDF</td><td><code>app/Services/Tenant/CustomerInvoicePdfService.php</code>, <code>resources/views/invoices/pdf.blade.php</code></td></tr><tr><td>Controller</td><td><code>app/Http/Controllers/Tenant/Api/V1/CustomerInvoiceController.php</code></td></tr><tr><td>Requests</td><td><code>app/Http/Requests/Tenant/Api/V1/CustomerInvoice/*</code></td></tr><tr><td>Resources</td><td><code>app/Http/Resources/Tenant/Api/V1/CustomerInvoice/*</code></td></tr><tr><td>Policy</td><td><code>app/Policies/CustomerInvoicePolicy.php</code></td></tr><tr><td>Events</td><td><code>app/Events/CustomerInvoice*.php</code></td></tr><tr><td>Subscriber</td><td><code>app/Listeners/CustomerInvoiceEventSubscriber.php</code> (audit + assignment notification)</td></tr><tr><td>Notifications</td><td><code>app/Notifications/Tenant/CustomerInvoice/CustomerInvoiceAssignedNotification.php</code></td></tr><tr><td>Link rules</td><td><code>LinkableContact</code>, <code>LinkableCompany</code>, <code>LinkableReseller</code>, <code>EligibleInvoiceAssignee</code> — <code>quotation_id</code> is a plain tenant-scoped <code>Rule::exists()</code>, <strong>not</strong> gated by a <code>LinkableQuotation</code>-style entitlement rule; <code>reseller_id</code> requires Resellers entitlement + assignee scope via <code>LinkableReseller</code></td></tr><tr><td>Tests</td><td><code>tests/Feature/Tenant/CustomerInvoice/CustomerInvoiceTest.php</code>, <code>CustomerInvoiceRecurrenceTest.php</code></td></tr></tbody></table><h2 id="domain-notes" tabindex="-1">Domain notes <a class="header-anchor" href="#domain-notes" aria-label="Permalink to &quot;Domain notes&quot;">​</a></h2><ul><li><strong>No hard dependency</strong>: Invoices does <strong>not</strong> declare a <code>module_dependencies</code> row on Opportunities (unlike Quotations/Contracts) — it installs standalone from Marketplace.</li><li>Status machine lives on <code>CustomerInvoiceStatusEnum::allowedTransitions()</code> / <code>canTransitionTo()</code>: <code>draft → unpaid|cancelled</code>, <code>unpaid → cancelled|paid</code>, <code>paid</code>/<code>cancelled</code> are terminal. <code>paid</code> is set programmatically by <a href="/developer-guide/payments">Payments</a> posting/voiding or <a href="/developer-guide/credit-notes">Credit Notes</a> applying, via <code>CustomerInvoice::applyBalanceStatus()</code> / <code>recalculateBalanceFromAmounts()</code> — partial settlement keeps <code>unpaid</code>. This API only exposes user-driven <code>send</code> / <code>void</code> (cancel) / <code>status</code>.</li><li><strong><code>Unpaid → Cancelled</code> requires a zero ledger</strong> — <code>CustomerInvoiceService::void()</code> is the enforcement point (not just the enum): it throws <code>ValidationException</code> (422, <code>status</code>, naming the invoice number) if <code>amount_paid &gt; 0</code> (void the payments first) or <code>amount_credited &gt; 0</code> (applied credits can never be reversed). <code>CustomerInvoice::isVoidable()</code> mirrors this (Draft/Unpaid only, both amounts zero). <code>changeStatus()</code> (used by <code>POST …/status</code>) routes a <code>cancelled</code> target through <code>void()</code> rather than calling <code>transitionStatus()</code> directly, so both entry points share the same ledger guard.</li><li><code>CustomerInvoiceService::transitionStatus()</code> throws <code>ValidationException</code> (422, <code>status</code> field) for disallowed transitions.</li><li>Content updates (<code>PUT</code>) and line sync are <strong>draft-only</strong> via <code>CustomerInvoice::isEditable()</code> (<code>status === draft</code>). Assignment remains available after send via <code>POST …/assign</code>.</li><li><code>POST …/status</code> (<code>changeStatus</code>) maps target status to permissions in the controller: <code>unpaid</code> → <code>invoices.send</code>, <code>cancelled</code> → <code>invoices.void</code>, otherwise <code>invoices.update</code>. The form request itself only requires <code>invoices.update</code>; the controller&#39;s <code>Gate::authorize()</code> call adds the stricter check per target status.</li><li><code>send</code> / <code>void</code> / <code>view</code> / <code>update</code> policies are assignee-scoped (same pattern) unless the actor has <code>invoices.assign</code> or is superadmin.</li><li><code>send()</code> backfills <code>issue_date</code> to today if unset, then transitions to <code>sent</code>. If the invoice is a recurring <strong>series root</strong>, this also sets <code>recurrence_status=active</code>. <strong><code>recurrence_next_issue_on</code></strong> is the date the operator chose on the draft (required when recurring); the SPA auto-fills one frequency step from the issue date (or workspace today) via the same no-overflow rules as <code>CustomerInvoiceRecurrenceFrequencyEnum::nextDate()</code>, and the operator can override it. Send keeps it when it is after the issue date, otherwise it falls back to one frequency step from the issue date. <strong>Status-only</strong> — no outbound email delivery.</li><li><strong>Customer email:</strong> <code>POST …/email</code> (<code>invoices.send</code>, assignee-scoped, <code>throttle:billing-document-email</code>) delivers a branded message with optional PDF attachment via <code>BillingDocumentMailer</code> + <code>CustomerInvoiceEmailService</code>. Requires a sent invoice (<code>unpaid</code>/<code>paid</code>; draft/cancelled → 422). Resolves default <code>to</code> from contact then company (<code>ResolvesBillingDocumentRecipients</code>). Records <code>CustomerInvoiceActivityTypeEnum::Emailed</code> and tenant email logs (<code>customer_invoice.emailed</code>).</li><li>Recurring series live on <code>customer_invoices</code> (<code>is_recurring</code>, <code>recurrence_frequency</code>, <code>recurrence_status</code>, <code>recurrence_next_issue_on</code>, <code>recurrence_ends_on</code>, <code>recurrence_due_days</code>, <code>recurring_source_invoice_id</code>). The original invoice is the template. <code>invoices:generate-recurring</code> (daily) clones <strong>draft</strong> occurrences from the root’s current lines; children are not themselves recurring. <code>POST …/recurrence/stop</code> ends the series (<code>ended</code>) and optionally voids the latest unpaid generated invoice (<code>void_latest_unpaid</code>). Voiding the root also ends an active series.</li><li>Generator: <code>chunkById</code> over due series roots, per-series <code>try/catch</code>, <code>withTrashed()</code> uniqueness for <code>(tenant, source, issue_date)</code>, catch-up cap (<code>config(&#39;invoices.recurring_catchup_cap&#39;)</code>, default 52) and per-tenant time budget. Command returns <code>FAILURE</code> if any entitled tenant had a failed series. Remaining due periods run on the next daily tick.</li><li>PDF: <code>GET …/pdf</code> (<code>invoices.view</code>, assignee-scoped, <code>throttle:invoices-pdf</code>) renders a Dompdf document from <code>resources/views/invoices/pdf.blade.php</code> via <code>CustomerInvoicePdfService</code> on the fly (no stored <code>pdf_path</code>). Layout uses workspace <code>button_color</code>, embedded logo (base64 from branding disk), and invoice settings (<code>company_*</code>, <code>invoice_bank_*</code>, <code>invoice_payment_terms</code>, <code>invoice_default_notes</code>). Includes discount rows when <code>discount_total &gt; 0</code>, sanitized memo HTML, and a <strong>Payments received</strong> table for posted payment allocations. Shows a <strong>Partial</strong> chip when unpaid with partial payments. Totals / balance stay in a short right-aligned block; Notes / Terms are full-width blocks below so Dompdf paginates long HTML. Line-item body HTML is a block-level <code>.line-body</code> under each short pricing row. Cached (base64) by id + <code>updated_at</code> + settings fingerprint so database/Redis JSON stores stay valid UTF-8 and branding edits invalidate the cache. <code>send()</code> dispatches <code>WarmCustomerInvoicePdfJob</code> on the default queue.</li><li>Line items are fully replaced on create/update (<code>CustomerInvoiceService::syncLines()</code>); <code>CustomerInvoice::recalculateTotals()</code> delegates to <code>DocumentTotalsCalculator</code> for <code>subtotal</code> / <code>discount_total</code> / <code>tax_total</code> / <code>total</code> from persisted <code>CustomerInvoiceLine</code> rows plus document <code>line_discount_type</code>. Tax is calculated after line discounts. <code>balance_due</code> is then derived from <code>total - amount_paid - amount_credited</code> via <code>recalculateBalanceFromAmounts()</code>, called by <a href="/developer-guide/payments">Payments</a> on post/void and by <a href="/developer-guide/credit-notes">Credit Notes</a> on apply.</li><li>Shared line discounts use <code>DocumentDiscountTypeEnum</code> (<code>none</code>, <code>percent</code>, <code>fixed</code>) on the parent as <code>line_discount_type</code>; lines store <code>name</code>, optional <code>body</code>, optional <code>product_id</code> (<code>LinkableProduct</code>: Products entitled, <code>products.view</code> or superadmin, active non-trashed), and <code>discount_value</code>. Validation in <code>DocumentDiscountRules</code>. Memo <code>notes</code> accept sanitized HTML via <code>DocumentHtmlSanitizer</code>. Recurring generation copies <code>product_id</code> onto occurrence lines.</li><li><strong>Partial</strong> is UI-only: the SPA shows a Partial badge when <code>status === unpaid</code>, <code>amount_paid &gt; 0</code>, and <code>balance_due &gt; 0</code>. The API has no <code>partial</code> status — partial settlement keeps <code>unpaid</code> until the balance clears.</li><li>Assignee scoping via <code>ScopesToAssignee</code> with <code>invoices.assign</code>.</li><li><code>invoices.force.delete</code> is not granted to any default role — owner/superadmin only.</li><li><code>estimate_id</code> and <code>contract_id</code> are set by convert actions (<code>nullOnDelete</code>); <code>estimate_id</code> is <strong>unique</strong> when not null (one-shot estimate convert); <code>quotation_id</code> is <strong>not</strong> unique so contracts can bill more than once.</li><li>Auto-numbering: <code>CustomerInvoiceService::nextNumber()</code> reads the <code>invoices_number_prefix</code> tenant setting (default <code>INV-</code>), then zero-pads a running count (<code>CustomerInvoice::withTrashed()-&gt;count() + 1</code>) to 5 digits. <code>customer_invoices</code> has a <code>unique(tenant_id, number)</code> DB index; <code>create()</code> wraps the insert with the shared <code>RetriesOnDuplicateNumber</code> trait (<code>app/Services/Tenant/Concerns/RetriesOnDuplicateNumber.php</code>), retrying up to 3 times with a freshly generated number if two concurrent requests race to the same count-derived sequence. The same trait/index pattern is used by Payments, Credit Notes, and Estimates.</li><li>Overdue definition (shared by list <code>overdue=true</code> filter and <code>stats.overdue</code>): <code>due_date &lt; today</code>, <code>status</code> = <code>unpaid</code>, <code>balance_due &gt; 0</code>.</li></ul><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>invoices.view | create | update | delete | restore | force.delete | assign | send | void</span></span></code></pre></div><p>Routes use <code>module:invoices</code> then <code>can:invoices.*</code> / policies.</p><p>Catalog: slug <code>invoices</code>, category <code>billing</code>, <code>is_default_included = false</code>, <code>is_billable = false</code>, <code>sort_order = 10</code>, version <strong>1.8.0</strong>. Registered via <code>DefaultModuleRegistrar</code> migration (migrate-only); 1.5.0 added optional product line picker; 1.5.1 hardens linking + sanitizer; 1.6.0 adds <code>contract_id</code> for contract-created invoices; 1.6.1 adds unique nullable <code>estimate_id</code> for one-shot estimate convert; 1.7.0 dedicated record pages; 1.7.1 PDF long-notes pagination; 1.7.2 PDF long line-body pagination; 1.8.0 customer email delivery (<code>POST …/email</code>).</p><h2 id="api-tenant" tabindex="-1">API (tenant) <a class="header-anchor" href="#api-tenant" aria-label="Permalink to &quot;API (tenant)&quot;">​</a></h2><p>Base: <code>/api/tenant/v1</code> — full reference <a href="/api/tenant-v1-invoices">tenant-v1-invoices.md</a>.</p><h2 id="frontend" tabindex="-1">Frontend <a class="header-anchor" href="#frontend" aria-label="Permalink to &quot;Frontend&quot;">​</a></h2><p>SPA mirrors <strong>Quotations</strong> (table + create/edit page, record page) under the existing AppLayout — do not invent a parallel shell.</p><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Page</td><td><code>src/pages/invoices/</code> (<code>invoices-page.tsx</code>, <code>invoice-form-dialog.tsx</code>, <code>invoice-detail-sheet.tsx</code>)</td></tr><tr><td>Detail sheet tabs</td><td>Overview, Lines, Notes, Timeline — actions: download PDF, email customer (after send), stop recurring (active series root), assign, add note, send, cancel (<code>/void</code>), edit (draft only), delete. No <strong>accept</strong> action (Invoices has no <code>accepted</code> status). List/detail show <strong>Partial</strong> badge (display-only) when unpaid with partial payments.</td></tr><tr><td>Shared billing UI</td><td><code>src/components/billing/document-lines-editor.tsx</code>, <code>document-totals-panel.tsx</code>, <code>src/components/common/rich-text-editor.tsx</code>, <code>src/lib/billing/document-totals.ts</code>, <code>src/lib/sanitize-html.ts</code></td></tr><tr><td>Service</td><td><code>customerInvoiceService</code> in <code>src/api/services.ts</code></td></tr><tr><td>Types</td><td><code>CustomerInvoice*</code> in <code>src/types/api.ts</code> (kept distinct from the pre-existing Central <code>Invoice*</code> types)</td></tr><tr><td>Query keys</td><td><code>QUERY_KEYS.customerInvoices</code> / <code>customerInvoice(id)</code> / <code>customerInvoiceTimeline(id)</code> / <code>customerInvoiceStats</code></td></tr><tr><td>Permissions</td><td><code>PERMISSIONS.customerInvoices.*</code> (maps to <code>invoices.*</code> permission strings)</td></tr><tr><td>Nav</td><td>New <strong>Billing</strong> sidebar group (after Sales) — <code>permission: PERMISSIONS.customerInvoices.view</code>, <code>module: &#39;invoices&#39;</code>. Kept separate from the Central Billing nav.</td></tr><tr><td>Route</td><td><code>tenantRoutes.invoices = &#39;/invoices&#39;</code>, lazy-loaded in <code>App.tsx</code> behind <code>RequireAccess module=&quot;invoices&quot;</code></td></tr><tr><td>Notifications</td><td><code>src/notifications/modules/invoices.ts</code> — <code>customer_invoice.assigned</code> → <code>/invoices?invoice={id}</code></td></tr><tr><td>Playwright</td><td>One shared login, headed human workflow: validation, CRUD, Overview memo + activity notes/timeline, PDF, send/void, recurring generate + stop with optional void, shortcuts, trash. <code>e2e/pages/invoices.page.ts</code>, <code>e2e/tests/invoices/</code>, <code>npm run test:e2e:invoices</code> / <code>test:e2e:invoices:headed</code></td></tr></tbody></table><p>Production readiness: <a href="/deployment/invoices-production-readiness">Invoices 1.1.0</a>.</p><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/CustomerInvoice/CustomerInvoiceTest.php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/CustomerInvoice/CustomerInvoiceRecurrenceTest.php</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e:invoices</span></span></code></pre></div><h2 id="logging" tabindex="-1">Logging <a class="header-anchor" href="#logging" aria-label="Permalink to &quot;Logging&quot;">​</a></h2><ul><li>Spatie <code>LogsActivity</code> on <code>CustomerInvoice</code> (log name <code>customer_invoices</code>)</li><li>Domain <code>customer_invoice_activities</code> timeline</li><li><code>PlatformAuditService</code> via <code>CustomerInvoiceEventSubscriber</code></li><li>Recurring generate: <code>invoices.generate-recurring.series_failed</code>, <code>tenant_failed</code>, <code>time_budget_reached</code></li><li>PDF: <code>invoices.pdf.rendered</code>, <code>invoices.pdf.warm_failed</code></li></ul>`,21)])])}const m=o(c,[["render",n]]);export{h as __pageData,m as default};
