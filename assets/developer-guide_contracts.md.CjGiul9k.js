import{_ as t,o,c as a,a3 as i}from"./chunks/framework.BTQ4eCVb.js";const u=JSON.parse('{"title":"Contracts — Developer Guide","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Contracts — Developer Guide | EloSync Docs"}],["meta",{"property":"og:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"property":"og:url","content":"https://docs.elosync.com/developer-guide/contracts"}],["meta",{"property":"og:image","content":"https://docs.elosync.com/og-image.svg"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:title","content":"Contracts — Developer Guide | EloSync Docs"}],["meta",{"name":"twitter:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"name":"twitter:image","content":"https://docs.elosync.com/og-image.svg"}],["link",{"rel":"canonical","href":"https://docs.elosync.com/developer-guide/contracts"}]]},"headers":[],"relativePath":"developer-guide/contracts.md","filePath":"developer-guide/contracts.md","lastUpdated":1787485542000}'),n={name:"developer-guide/contracts.md"};function s(d,e,c,r,l,p){return o(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="contracts-—-developer-guide" tabindex="-1">Contracts — Developer Guide <a class="header-anchor" href="#contracts-—-developer-guide" aria-label="Permalink to &quot;Contracts — Developer Guide&quot;">​</a></h1><p>Mirror of the <a href="/developer-guide/opportunities">Opportunities developer guide</a> and <a href="/developer-guide/quotations">Quotations developer guide</a> (assignee scope, notes, domain timeline), kept lean: no line items, no pipeline/board — a contract is a single agreement record. Prefer copying those patterns over inventing new ones.</p><h2 id="backend-layout" tabindex="-1">Backend layout <a class="header-anchor" href="#backend-layout" aria-label="Permalink to &quot;Backend layout&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Models</td><td><code>app/Models/Contract.php</code>, <code>ContractNote</code>, <code>ContractActivity</code></td></tr><tr><td>Enums</td><td><code>ContractStatusEnum</code>, <code>ContractActivityTypeEnum</code></td></tr><tr><td>Support</td><td><code>app/Support/Billing/DocumentHtmlSanitizer.php</code>, <code>DocumentDiscountRules.php</code> (notes max length), <code>QuotationInvoiceGuard.php</code></td></tr><tr><td>Service</td><td><code>app/Services/Tenant/ContractService.php</code> (+ <code>ScopesToAssignee</code>)</td></tr><tr><td>Controller</td><td><code>app/Http/Controllers/Tenant/Api/V1/ContractController.php</code></td></tr><tr><td>Requests</td><td><code>app/Http/Requests/Tenant/Api/V1/Contract/*</code></td></tr><tr><td>Resources</td><td><code>app/Http/Resources/Tenant/Api/V1/Contract/*</code></td></tr><tr><td>Policy</td><td><code>app/Policies/ContractPolicy.php</code></td></tr><tr><td>Events</td><td><code>app/Events/Contract*.php</code></td></tr><tr><td>Subscriber</td><td><code>app/Listeners/ContractEventSubscriber.php</code> (audit + assignment notification)</td></tr><tr><td>Notifications</td><td><code>app/Notifications/Tenant/Contract/ContractAssignedNotification.php</code></td></tr><tr><td>Link rules</td><td><code>LinkableQuotation</code> (checks Quotations entitlement + assignee scope + same <code>opportunity_id</code>), <code>EligibleOpportunityAssignee</code></td></tr><tr><td>Dependency migration</td><td><code>database/migrations/2026_07_30_230005_add_contracts_opportunities_dependency.php</code> (mirrors the Quotations → Opportunities dependency)</td></tr><tr><td>Tests</td><td><code>tests/Feature/Tenant/Contract/ContractTest.php</code>, <code>tests/Feature/Central/Module/ContractsModuleDependencyTest.php</code></td></tr></tbody></table><h2 id="domain-notes" tabindex="-1">Domain notes <a class="header-anchor" href="#domain-notes" aria-label="Permalink to &quot;Domain notes&quot;">​</a></h2><ul><li><strong>Hard dependency</strong>: Contracts declares a required <code>module_dependencies</code> row on Opportunities — Marketplace install is blocked until Opportunities is entitled.</li><li><strong>Soft optional dependency</strong>: <code>quotation_id</code> is nullable and validated by <code>LinkableQuotation</code> — it fails validation when the Quotations module is not entitled for the tenant, when the quotation is soft-deleted, when the actor cannot view that quotation (same assignee-scope pattern as <code>LinkableCompanyForOpportunity</code>), or when the quotation’s <code>opportunity_id</code> does not match the contract’s opportunity.</li><li><strong>Create invoice</strong> (<code>ContractService::createInvoice()</code>): soft Invoices entitlement. Active contracts only (<code>isBillable()</code>). Repeatable with <code>lockForUpdate</code> on the contract. Copies quotation lines when present, else a lump-sum line from <code>value</code>. Sets <code>contract_id</code> and optional <code>quotation_id</code>. Second and later bills require <code>acknowledge_repeat_billing=true</code>. Does not unique-index <code>quotation_id</code>. Fires <code>ContractInvoiceCreated</code>. <code>QuotationInvoiceGuard</code> is used only for the detail flag <code>quotation_already_invoiced</code> (UI warning).</li><li>Status machine lives on <code>ContractStatusEnum::allowedTransitions()</code> / <code>canTransitionTo()</code>: <code>draft → active → expired|terminated</code> (also <code>draft → terminated</code>). <code>ContractService::changeStatus()</code> throws <code>ValidationException</code> (422, <code>status</code> field) for disallowed transitions, including re-entering the same status.</li><li>Content updates are <strong>draft-only</strong> (<code>Contract::isEditable()</code>); assignment stays available via <code>POST …/assign</code> after activate.</li><li>No line items / totals — <code>value</code> is a single optional decimal field.</li><li>Memo <code>description</code> and <code>notes</code> accept sanitized HTML via <code>DocumentHtmlSanitizer</code> (same allowlist as billing document notes; empty HTML is stored as <code>null</code>). Timeline comments (<code>contract_notes</code>) remain plain text.</li><li>Assignee scoping via <code>ScopesToAssignee</code> with <code>contracts.assign</code>.</li><li><code>contracts.force.delete</code> is not granted to any default role — owner/superadmin only.</li></ul><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>contracts.view | create | update | delete | restore | force.delete | assign | convert</span></span></code></pre></div><p>Routes use <code>module:contracts</code> then <code>can:contracts.*</code> / policies.</p><p>Catalog: slug <code>contracts</code>, category <code>sales</code>, <code>is_default_included = false</code>, <code>is_billable = false</code>, <code>sort_order = 60</code>, version <strong>1.2.1</strong>. Registered via <code>DefaultModuleRegistrar</code> migration (migrate-only). 1.1.0 added opportunity auto-fill + HTML memos; 1.2.0 adds create-invoice (soft Invoices entitlement); 1.2.1 requires <code>acknowledge_repeat_billing</code> for second+ bills. Production readiness: <a href="/deployment/sales-document-convert-production-readiness">Sales document convert</a>.</p><h2 id="api-tenant" tabindex="-1">API (tenant) <a class="header-anchor" href="#api-tenant" aria-label="Permalink to &quot;API (tenant)&quot;">​</a></h2><p>Base: <code>/api/tenant/v1</code> — full reference <a href="/api/tenant-v1-contracts">tenant-v1-contracts.md</a>.</p><h2 id="frontend" tabindex="-1">Frontend <a class="header-anchor" href="#frontend" aria-label="Permalink to &quot;Frontend&quot;">​</a></h2><p>SPA should mirror <strong>Opportunities</strong> / <strong>Quotations</strong> (table + create/edit page, record page) under the existing AppLayout — do not invent a parallel shell. Create form: selecting an opportunity auto-fills party, value, currency, and title (when empty); assignee is copied only when that user appears in the eligible picker (<code>filterLeadAssigneeOptions</code> — owners/suspended/excluded are omitted). Quotation auto-links only when that opportunity has exactly one quotation. Description and notes use the shared TipTap <code>RichTextEditor</code>. Store allows the creating actor to pass <code>assigned_to</code> as themselves; otherwise <code>EligibleOpportunityAssignee</code> applies. Service still defaults <code>assigned_to</code> to the actor when the field is null.</p><table tabindex="0"><thead><tr><th>Piece</th><th>Path (expected)</th></tr></thead><tbody><tr><td>Page</td><td><code>src/pages/contracts/</code></td></tr><tr><td>Form / detail</td><td>create/edit page + record page (Overview, Notes, Activity)</td></tr><tr><td>Service</td><td><code>contractService</code> in <code>src/api/services.ts</code></td></tr><tr><td>Nav</td><td><code>permission: contracts.view</code>, <code>module: &#39;contracts&#39;</code> (Sales)</td></tr><tr><td>Playwright</td><td><code>e2e/pages/contracts.page.ts</code>, <code>e2e/tests/contracts/</code>, <code>npm run test:e2e:contracts</code></td></tr></tbody></table><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/Contract/ContractTest.php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Central/Module/ContractsModuleDependencyTest.php</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e:contracts</span></span></code></pre></div><h2 id="logging" tabindex="-1">Logging <a class="header-anchor" href="#logging" aria-label="Permalink to &quot;Logging&quot;">​</a></h2><ul><li>Spatie <code>LogsActivity</code> on <code>Contract</code> (log name <code>contracts</code>)</li><li>Domain <code>contract_activities</code> timeline</li><li><code>PlatformAuditService</code> via <code>ContractEventSubscriber</code></li></ul>`,19)])])}const g=t(n,[["render",s]]);export{u as __pageData,g as default};
