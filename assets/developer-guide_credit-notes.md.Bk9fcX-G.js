import{_ as t,o,c as d,a3 as i}from"./chunks/framework.BTQ4eCVb.js";const m=JSON.parse('{"title":"Credit Notes — Developer Guide","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Credit Notes — Developer Guide | EloSync Docs"}],["meta",{"property":"og:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"property":"og:url","content":"https://docs.elosync.com/developer-guide/credit-notes"}],["meta",{"property":"og:image","content":"https://docs.elosync.com/og-image.svg"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:title","content":"Credit Notes — Developer Guide | EloSync Docs"}],["meta",{"name":"twitter:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"name":"twitter:image","content":"https://docs.elosync.com/og-image.svg"}],["link",{"rel":"canonical","href":"https://docs.elosync.com/developer-guide/credit-notes"}]]},"headers":[],"relativePath":"developer-guide/credit-notes.md","filePath":"developer-guide/credit-notes.md","lastUpdated":1787680328000}'),s={name:"developer-guide/credit-notes.md"};function a(c,e,r,n,l,p){return o(),d("div",null,[...e[0]||(e[0]=[i(`<h1 id="credit-notes-—-developer-guide" tabindex="-1">Credit Notes — Developer Guide <a class="header-anchor" href="#credit-notes-—-developer-guide" aria-label="Permalink to &quot;Credit Notes — Developer Guide&quot;">​</a></h1><p>Mirror of the <a href="/developer-guide/payments">Payments developer guide</a> (assignee scope, notes, domain timeline, hard dependency on Invoices), with a <code>lines</code> child table (subtotal/tax/total) instead of Payments&#39; <code>allocations</code>.</p><blockquote><p><strong>Naming:</strong> the backend model is <code>CustomerCreditNote</code> (table <code>customer_credit_notes</code>) — distinct from Central&#39;s own platform-billing <code>credit_notes</code> ledger table (credits the platform issues <em>to</em> a tenant against its own module-subscription invoices). Frontend mirrors this with <code>customerCreditNoteService</code> / <code>PERMISSIONS.customerCreditNotes</code> / <code>QUERY_KEYS.customerCreditNotes</code>.</p></blockquote><h2 id="backend-layout" tabindex="-1">Backend layout <a class="header-anchor" href="#backend-layout" aria-label="Permalink to &quot;Backend layout&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Models</td><td><code>app/Models/CustomerCreditNote.php</code>, <code>CustomerCreditNoteLine</code>, <code>CustomerCreditNoteNote</code>, <code>CustomerCreditNoteActivity</code></td></tr><tr><td>Enums</td><td><code>CustomerCreditNoteStatusEnum</code>, <code>CustomerCreditNoteActivityTypeEnum</code></td></tr><tr><td>Service</td><td><code>app/Services/Tenant/CustomerCreditNoteService.php</code> (+ <code>ScopesToAssignee</code>)</td></tr><tr><td>Controller</td><td><code>app/Http/Controllers/Tenant/Api/V1/CustomerCreditNoteController.php</code></td></tr><tr><td>Requests</td><td><code>app/Http/Requests/Tenant/Api/V1/CustomerCreditNote/*</code></td></tr><tr><td>Resources</td><td><code>app/Http/Resources/Tenant/Api/V1/CustomerCreditNote/*</code></td></tr><tr><td>Policy</td><td><code>app/Policies/CustomerCreditNotePolicy.php</code></td></tr><tr><td>Events</td><td><code>app/Events/CustomerCreditNote*.php</code></td></tr><tr><td>Subscriber</td><td><code>app/Listeners/CustomerCreditNoteEventSubscriber.php</code> (audit + assignment notification)</td></tr><tr><td>Notifications</td><td><code>app/Notifications/Tenant/CustomerCreditNote/CustomerCreditNoteAssignedNotification.php</code></td></tr><tr><td>Link rules</td><td><code>LinkableContact</code>, <code>LinkableCompany</code>, <code>EligibleCreditNoteAssignee</code> — <code>customer_invoice_id</code> is a plain tenant-scoped <code>Rule::exists()</code> on <code>CustomerInvoice</code></td></tr><tr><td>Factories</td><td><code>CustomerCreditNoteFactory</code>, <code>CustomerCreditNoteLineFactory</code>, <code>CustomerCreditNoteNoteFactory</code>, <code>CustomerCreditNoteActivityFactory</code></td></tr><tr><td>Tests</td><td><code>tests/Feature/Tenant/CustomerCreditNote/CustomerCreditNoteTest.php</code></td></tr></tbody></table><h2 id="domain-notes" tabindex="-1">Domain notes <a class="header-anchor" href="#domain-notes" aria-label="Permalink to &quot;Domain notes&quot;">​</a></h2><ul><li><strong>Hard dependency</strong>: Credit Notes declares a required <code>module_dependencies</code> row on Invoices (<code>database/migrations/2026_07_31_222006_add_credit_notes_invoices_dependency.php</code>) — Marketplace install is blocked until Invoices is entitled, same pattern as Payments → Invoices.</li><li>Status machine lives on <code>CustomerCreditNoteStatusEnum::allowedTransitions()</code> / <code>canTransitionTo()</code>: <code>draft → issued</code>, <code>draft → void</code>, <code>issued → applied</code>, <code>issued → void</code>; <code>applied</code> and <code>void</code> are terminal.</li><li><code>CustomerCreditNoteService::transitionStatus()</code> throws <code>ValidationException</code> (422, <code>status</code> field) for disallowed transitions; used by <code>issue()</code>, <code>apply()</code>, and <code>void()</code>.</li><li><code>issue()</code> backfills <code>issue_date</code> to today if it wasn&#39;t already set.</li><li><code>apply()</code> locks the linked invoice with <code>CustomerInvoice::query()-&gt;lockForUpdate()</code> (deliberately <strong>not</strong> <code>withTrashed()</code> — the target invoice must still be active) and validates before writing anything, throwing on <code>status</code>: invoice must exist, its status must be <code>sent</code>/<code>partial</code>, and the credit note&#39;s <code>total</code> must not exceed the invoice&#39;s <code>balance_due</code> (0.01 float tolerance). It then adds <code>total</code> to <code>CustomerInvoice::amount_credited</code> and calls <code>CustomerInvoice::recalculateBalanceFromAmounts()</code> — this recomputes <code>balance_due</code> <strong>and can advance the invoice <code>status</code></strong> (<code>sent → partial|paid</code>), the same as Payments posting. It also records a <code>credited</code> activity on the invoice itself.</li><li><code>void()</code> is a pure status transition — it does <strong>not</strong> reverse any invoice balance, because it&#39;s only reachable from <code>draft</code>/<code>issued</code> (before <code>amount_credited</code> has been touched).</li><li>Content updates (<code>PUT</code>) — including replacing the full <code>lines</code> array — are <strong>draft-only</strong> via <code>CustomerCreditNoteService::isEditable()</code> (<code>status === draft</code>, same guard used for delete). Assignment remains available after issuing via <code>POST …/assign</code>.</li><li>Lines are a first-class child table (<code>customer_credit_note_lines</code>), not embedded JSON — each row is <code>{ description, quantity, unit_price, tax_rate, sort_order }</code>. <code>subtotal</code>/<code>tax_total</code>/<code>total</code> are recomputed server-side from lines on create/update, same as Invoices.</li><li>Assignee scoping via <code>ScopesToAssignee</code> with <code>credit-notes.assign</code>.</li><li><code>credit-notes.force.delete</code> is not granted to any default role — owner/superadmin only.</li><li><code>contact_id</code> / <code>company_id</code> are optional and validated for module entitlement + assignee scope (<code>LinkableContact</code> / <code>LinkableCompany</code>); when omitted, the frontend form pre-fills them from the selected invoice, but the backend does not auto-default them — a blank value is stored as <code>null</code>.</li><li>Auto-numbering: <code>CustomerCreditNoteService::nextNumber()</code> reads the <code>credit_notes_number_prefix</code> tenant setting (default <code>CN-</code>), then zero-pads a running count to 5 digits — same pattern as Invoices/Payments. Prefix is editable under <strong>Settings → General → Document number prefixes</strong>. <code>customer_credit_notes</code> has a <code>unique(tenant_id, number)</code> DB index; <code>create()</code> retries up to 3 times via the shared <code>RetriesOnDuplicateNumber</code> trait on a duplicate-key collision.</li></ul><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>credit-notes.view | create | update | delete | restore | force.delete | assign | issue | apply | void</span></span></code></pre></div><p>Routes use <code>module:credit-notes</code> then <code>can:credit-notes.*</code> / policies.</p><p>Catalog: slug <code>credit-notes</code>, category <code>billing</code>, <code>is_default_included = false</code>, <code>is_billable = false</code>, <code>sort_order = 30</code>. Registered via <code>DefaultModuleRegistrar</code> migration (migrate-only), with a follow-up migration inserting the <code>module_dependencies</code> row on <code>invoices</code>.</p><h2 id="api-tenant" tabindex="-1">API (tenant) <a class="header-anchor" href="#api-tenant" aria-label="Permalink to &quot;API (tenant)&quot;">​</a></h2><p>Base: <code>/api/tenant/v1</code> — full reference <a href="/api/tenant-v1-credit-notes">tenant-v1-credit-notes.md</a>.</p><h2 id="frontend" tabindex="-1">Frontend <a class="header-anchor" href="#frontend" aria-label="Permalink to &quot;Frontend&quot;">​</a></h2><p>SPA mirrors <strong>Invoices</strong>/<strong>Payments</strong> (table + create/edit page, record page) under the existing AppLayout — do not invent a parallel shell.</p><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Page</td><td><code>src/pages/credit-notes/</code> (<code>credit-notes-page.tsx</code>, <code>credit-note-form-dialog.tsx</code>, <code>credit-note-detail-sheet.tsx</code>)</td></tr><tr><td>Detail sheet</td><td>Overview, linked invoice, contact/company, line items, notes, timeline — actions: assign, add note, issue, apply, void, edit (draft only), delete</td></tr><tr><td>Form dialog</td><td>Invoice picker (drives default currency/contact/company), title, notes, issue date, contact/company/assignee pickers, and a line-items editor (<code>useFieldArray</code>) with live subtotal/tax/total preview</td></tr><tr><td>Service</td><td><code>customerCreditNoteService</code> in <code>src/api/services.ts</code></td></tr><tr><td>Types</td><td><code>CustomerCreditNote*</code> in <code>src/types/api.ts</code></td></tr><tr><td>Query keys</td><td><code>QUERY_KEYS.customerCreditNotes</code> / <code>customerCreditNote(id)</code> / <code>customerCreditNoteTimeline(id)</code> / <code>customerCreditNoteStats</code></td></tr><tr><td>Permissions</td><td><code>PERMISSIONS.customerCreditNotes.*</code> (maps to <code>credit-notes.*</code> permission strings)</td></tr><tr><td>Nav</td><td><strong>Billing</strong> sidebar group, after Payments — <code>permission: PERMISSIONS.customerCreditNotes.view</code>, <code>module: &#39;credit-notes&#39;</code></td></tr><tr><td>Route</td><td><code>tenantRoutes.creditNotes = &#39;/credit-notes&#39;</code>, lazy-loaded in <code>App.tsx</code> behind <code>RequireAccess module=&quot;credit-notes&quot;</code></td></tr><tr><td>Notifications</td><td><code>src/notifications/modules/credit-notes.ts</code> — <code>customer_credit_note.assigned</code> → <code>/credit-notes?credit-note={id}</code></td></tr><tr><td>Cross-link</td><td>Invoice record page shows a &quot;Credit notes&quot; link to <code>/credit-notes?invoice={id}</code> when the Credit Notes module + <code>credit-notes.view</code> are both present</td></tr><tr><td>Playwright</td><td><code>e2e/pages/credit-notes.page.ts</code>, <code>e2e/tests/credit-notes/</code>, <code>npm run test:e2e:credit-notes</code></td></tr></tbody></table><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/CustomerCreditNote/CustomerCreditNoteTest.php</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e:credit-notes</span></span></code></pre></div><h2 id="logging" tabindex="-1">Logging <a class="header-anchor" href="#logging" aria-label="Permalink to &quot;Logging&quot;">​</a></h2><ul><li>Spatie <code>LogsActivity</code> on <code>CustomerCreditNote</code> (log name <code>customer_credit_notes</code>)</li><li>Domain <code>customer_credit_note_activities</code> timeline</li><li><code>PlatformAuditService</code> via <code>CustomerCreditNoteEventSubscriber</code></li></ul>`,20)])])}const h=t(s,[["render",a]]);export{m as __pageData,h as default};
