import{_ as t,o,c as d,a3 as s}from"./chunks/framework.BlfGB29P.js";const u=JSON.parse('{"title":"Expenses — Developer Guide","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Expenses — Developer Guide | EloSync Docs"}],["meta",{"property":"og:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"property":"og:url","content":"https://docs.elosync.com/developer-guide/expenses"}],["meta",{"property":"og:image","content":"https://docs.elosync.com/og-image.svg"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:title","content":"Expenses — Developer Guide | EloSync Docs"}],["meta",{"name":"twitter:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"name":"twitter:image","content":"https://docs.elosync.com/og-image.svg"}],["link",{"rel":"canonical","href":"https://docs.elosync.com/developer-guide/expenses"}]]},"headers":[],"relativePath":"developer-guide/expenses.md","filePath":"developer-guide/expenses.md","lastUpdated":1787049516000}'),a={name:"developer-guide/expenses.md"};function r(n,e,c,i,p,l){return o(),d("div",null,[...e[0]||(e[0]=[s(`<h1 id="expenses-—-developer-guide" tabindex="-1">Expenses — Developer Guide <a class="header-anchor" href="#expenses-—-developer-guide" aria-label="Permalink to &quot;Expenses — Developer Guide&quot;">​</a></h1><p>Simplified mirror of <a href="/developer-guide/purchase-orders">Purchase Orders</a> / <a href="/developer-guide/estimates">Estimates</a> (numbering, status machine, assignee scoping, notes, domain timeline) — no line-item child table (single <code>amount</code> + <code>tax_amount</code> MVP), and <strong>no</strong> hard module dependencies. <code>vendor_id</code> and <code>purchase_order_id</code> are both nullable soft links, validated only when the corresponding module is entitled.</p><h2 id="backend-layout" tabindex="-1">Backend layout <a class="header-anchor" href="#backend-layout" aria-label="Permalink to &quot;Backend layout&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Models</td><td><code>app/Models/Expense.php</code>, <code>ExpenseCategory</code>, <code>ExpenseNote</code>, <code>ExpenseActivity</code></td></tr><tr><td>Enums</td><td><code>ExpenseStatusEnum</code>, <code>ExpenseActivityTypeEnum</code></td></tr><tr><td>Service</td><td><code>app/Services/Tenant/ExpenseService.php</code> (+ <code>ScopesToAssignee</code>, <code>RetriesOnDuplicateNumber</code>), <code>ExpenseCategoryService</code>, <code>ExpenseCategorySeederService</code></td></tr><tr><td>Controller</td><td><code>app/Http/Controllers/Tenant/Api/V1/ExpenseController.php</code>, <code>ExpenseCategoryController</code></td></tr><tr><td>Requests</td><td><code>app/Http/Requests/Tenant/Api/V1/Expense/*</code>, <code>ExpenseCategory/*</code></td></tr><tr><td>Resources</td><td><code>app/Http/Resources/Tenant/Api/V1/Expense/*</code>, <code>ExpenseCategory/*</code></td></tr><tr><td>Policy</td><td><code>app/Policies/ExpensePolicy.php</code>, <code>ExpenseCategoryPolicy</code> (maps to <code>expenses.*</code>)</td></tr><tr><td>Events</td><td><code>app/Events/Expense*.php</code></td></tr><tr><td>Subscriber</td><td><code>app/Listeners/ExpenseEventSubscriber.php</code> (audit + assignment notification)</td></tr><tr><td>Notifications</td><td><code>app/Notifications/Tenant/Expense/ExpenseAssignedNotification.php</code></td></tr><tr><td>Link rules</td><td><code>app/Rules/LinkableVendor.php</code> (reused), <code>app/Rules/LinkablePurchaseOrder.php</code> — both optional, tenant-scoped, module-entitlement-checked</td></tr><tr><td>Assignee rule</td><td><code>app/Rules/EligibleExpenseAssignee.php</code></td></tr><tr><td>Factories</td><td><code>ExpenseFactory</code>, <code>ExpenseCategoryFactory</code>, <code>ExpenseNoteFactory</code>, <code>ExpenseActivityFactory</code></td></tr><tr><td>Tests</td><td><code>tests/Feature/Tenant/Expense/ExpenseTest.php</code>, <code>ExpenseCategoryTest.php</code></td></tr><tr><td>Migrations</td><td><code>database/migrations/2026_08_01_130000_create_expenses_table.php</code> … <code>130005_add_purchase_orders_convert_permission.php</code>; <code>2026_08_13_171652</code>+ expense categories + catalog bump <code>1.0.0 → 1.1.0</code></td></tr></tbody></table><h2 id="domain-notes" tabindex="-1">Domain notes <a class="header-anchor" href="#domain-notes" aria-label="Permalink to &quot;Domain notes&quot;">​</a></h2><ul><li><strong>No hard module dependency</strong>: Expenses has no <code>module_dependencies</code> row — it&#39;s installable standalone, unlike Purchase Orders → Vendors. <code>vendor_id</code> / <code>purchase_order_id</code> are both nullable columns.</li><li><strong>Tenant categories</strong>: <code>expense_categories</code> lookup (name, slug, <code>sort_order</code>, <code>is_active</code>, soft deletes). <code>expenses.category_id</code> is a nullable FK. Category CRUD reuses <code>expenses.view|create|update|delete|restore|force.delete</code> — no <code>expense-categories.*</code> family. <code>ExpenseCategorySeederService::ensureDefaults()</code> lazily inserts Travel / Office / Software / Utilities / Other (slugs <code>travel|office|software|utilities|other</code>) on first list/create/PO convert; lazy seed does not write activity. Starter slugs are immutable on update. <strong>Other</strong> cannot be soft- or force-deleted (422). Listing does not restore deleted Travel/Office/Software/Utilities; only a missing/trashed <strong>Other</strong> is restored (PO convert / create default). Delete/forceDelete is also blocked while any expenses (including trashed, for force) still reference the category. Spatie log name <code>expense-categories</code>. Catalog version <strong>1.1.0</strong>.</li><li>Status machine lives on <code>ExpenseStatusEnum::allowedTransitions()</code> / <code>canTransitionTo()</code>: <code>draft → submitted|cancelled</code>, <code>submitted → approved|rejected|cancelled</code>, <code>approved → paid</code>, <code>rejected</code>/<code>paid</code>/<code>cancelled</code> are terminal. <code>ExpenseService::transitionStatus()</code> throws <code>ValidationException</code> (422, <code>status</code> field) for disallowed transitions.</li><li>Content updates (<code>PUT</code>) are <strong>draft-only</strong> via <code>Expense::isEditable()</code> (<code>status === draft</code>). Assignment remains available after submit via <code>POST …/assign</code>.</li><li><code>submit()</code> / <code>cancel()</code> are assignee-scoped in <code>ExpensePolicy</code> (same as <code>view</code> / <code>update</code>) unless the actor has <code>expenses.assign</code> or is superadmin. <code>approve()</code> / <code>reject()</code> / <code>pay()</code> are <strong>not</strong> assignee-scoped — any actor with the specific permission can act, modeling an approver distinct from the submitter.</li><li><code>POST …/status</code> route middleware requires <code>expenses.update</code>; the controller then re-checks the specific gate per target status (<code>submitted</code> → <code>submit</code>, <code>approved</code> → <code>approve</code>, <code>rejected</code> → <code>reject</code>, <code>paid</code> → <code>pay</code>, <code>cancelled</code> → <code>cancel</code>, otherwise <code>update</code>) before delegating to <code>ExpenseService::changeStatus()</code> — same pattern as Purchase Orders.</li><li><code>vendor_id</code> validated via <code>LinkableVendor</code> (reused from Vendors/Purchase Orders — always optional here), <code>purchase_order_id</code> via the new <code>LinkablePurchaseOrder</code>. Both fail closed: null/absent value always passes; a non-null value requires the module to be entitled, the record to belong to the tenant, and (unless the actor has the <code>*.assign</code> permission or is superadmin) the record to be assigned to the actor.</li><li>No line-item child table — <code>amount</code> and <code>tax_amount</code> are plain decimal columns set directly from the request; there&#39;s no server-side computed total (the frontend renders <code>amount + tax_amount</code> for display).</li><li><code>expenses.force.delete</code> is not granted to any default role — owner/superadmin only.</li><li>Auto-numbering: <code>ExpenseService::nextNumber()</code> reads the <code>expenses_number_prefix</code> tenant setting (default <code>EXP-</code>), then zero-pads a running count to 5 digits — same pattern as Purchase Orders/Estimates/Invoices/Payments. Exposed via <code>PUT /settings</code> (<code>UpdateTenantSettingsRequest</code>). <code>expenses</code> has a <code>unique(tenant_id, number)</code> DB index; <code>create()</code> retries up to 3 times via the shared <code>RetriesOnDuplicateNumber</code> trait on a duplicate-key collision.</li></ul><h2 id="convert-from-purchase-order-soft" tabindex="-1">Convert-from-Purchase-Order (soft) <a class="header-anchor" href="#convert-from-purchase-order-soft" aria-label="Permalink to &quot;Convert-from-Purchase-Order (soft)&quot;">​</a></h2><p><code>PurchaseOrderService::convertToExpense()</code> mirrors <code>EstimateService::convert()</code>&#39;s pattern but the module check is <strong>soft</strong> (no <code>module_dependencies</code> row) rather than a hard dependency gate:</p><ol><li>Resolves the purchase order&#39;s tenant and checks <code>EntitlementService::hasModule($tenant, &#39;expenses&#39;)</code> — throws a 422 <code>ValidationException</code> (<code>purchase_order</code> field) if Expenses isn&#39;t entitled, rather than a hard 403 at the module-middleware layer.</li><li>Checks for an existing <code>Expense::withTrashed()-&gt;where(&#39;purchase_order_id&#39;, $purchaseOrder-&gt;id)</code> — throws a 422 if one already exists (one-time).</li><li>Checks <code>PurchaseOrder::isConvertible()</code> (status is <code>sent</code>, <code>partially_received</code>, or <code>received</code>) — throws a 422 otherwise. Draft orders haven&#39;t incurred real spend yet; cancelled orders shouldn&#39;t become payable expenses.</li><li>Creates a <strong>draft</strong> <code>Expense</code> inside a DB transaction: <code>title</code> = PO title, <code>category_id</code> defaults to the seeded <strong>Other</strong> category, <code>amount</code> = PO <code>total</code>, <code>tax_amount</code> = PO <code>tax_total</code>, <code>currency</code>/<code>vendor_id</code>/<code>assigned_to</code>/<code>notes</code> copied from the PO, <code>purchase_order_id</code> = PO id, <code>expense_date</code> = today.</li><li>Records a <code>PurchaseOrderActivityTypeEnum::Converted</code> activity on the purchase order and fires <code>PurchaseOrderConverted</code>.</li></ol><p>Exposed via <code>POST /purchase-orders/{purchaseOrder}/convert</code>, gated by <code>middleware(&#39;can:purchase-orders.convert&#39;)</code> at the route level (an ordinary Spatie permission, not a module-dependency check) plus <code>Gate::authorize(&#39;convert&#39;, $purchaseOrder)</code> in the controller. <code>PurchaseOrder::convertedExpense()</code> is a <code>hasOne(Expense::class)</code> relation; <code>ListPurchaseOrderResource</code> exposes <code>converted_expense_id</code> (null until converted) so the frontend can hide the button and show a link instead.</p><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>expenses.view | create | update | delete | restore | force.delete | assign | submit | approve | reject | pay | cancel</span></span>
<span class="line"><span>purchase-orders.convert</span></span></code></pre></div><p>Routes use <code>module:expenses</code> then <code>can:expenses.*</code> / policies. The convert route lives under the existing <code>module:purchase-orders</code> group and only needs <code>can:purchase-orders.convert</code> — the Expenses module check happens in the service layer (soft), not route middleware (hard).</p><p>Catalog: slug <code>expenses</code>, category <code>purchasing</code>, <code>is_default_included = false</code>, <code>is_billable = false</code>, <code>sort_order = 30</code>, version <strong>1.1.0</strong>. Registered via <code>DefaultModuleRegistrar</code> migration (migrate-only) — <strong>no</strong> <code>module_dependencies</code> row. Category CRUD is a MINOR bump (<code>1.0.0 → 1.1.0</code>).</p><h2 id="api-tenant" tabindex="-1">API (tenant) <a class="header-anchor" href="#api-tenant" aria-label="Permalink to &quot;API (tenant)&quot;">​</a></h2><p>Base: <code>/api/tenant/v1</code> — full reference <a href="/api/tenant-v1-expenses">tenant-v1-expenses.md</a>. Convert endpoint documented in <a href="/api/tenant-v1-purchase-orders">tenant-v1-purchase-orders.md</a>.</p><h2 id="frontend" tabindex="-1">Frontend <a class="header-anchor" href="#frontend" aria-label="Permalink to &quot;Frontend&quot;">​</a></h2><p>SPA mirrors <strong>Purchase Orders</strong> (table + form dialog, detail sheet) under the existing AppLayout — do not invent a parallel shell.</p><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Page</td><td><code>src/pages/expenses/</code> (<code>expenses-page.tsx</code>, <code>expense-form-dialog.tsx</code>, <code>expense-detail-sheet.tsx</code>, <code>expense-categories-dialog.tsx</code>)</td></tr><tr><td>Detail sheet</td><td>Overview (category name, amount/tax/total, date, assignee, related vendor/PO), notes, timeline — actions: assign, add note, submit, approve, reject, mark as paid, cancel, edit (draft only), delete</td></tr><tr><td>Form dialog</td><td>Title, category picker (<code>category_id</code>, active categories), amount, tax amount, currency, expense date, notes, and <strong>conditional</strong> vendor / purchase order pickers (<code>SearchableSelect</code>) shown only when <code>hasModule(&#39;vendors&#39;)</code> / <code>hasModule(&#39;purchase-orders&#39;)</code> is true</td></tr><tr><td>Service</td><td><code>expenseService</code> + <code>expenseCategoryService</code> in <code>src/api/services.ts</code>; <code>purchaseOrderService.convert()</code> for the PO action</td></tr><tr><td>Types</td><td><code>Expense*</code> / <code>ExpenseCategory*</code> in <code>src/types/api.ts</code>; <code>PurchaseOrder.converted_expense_id</code> added for the convert UI</td></tr><tr><td>Query keys</td><td><code>QUERY_KEYS.expenses</code> / <code>expense(id)</code> / <code>expenseTimeline(id)</code> / <code>expenseStats</code> / <code>expenseCategories</code></td></tr><tr><td>Permissions</td><td><code>PERMISSIONS.expenses.*</code> (maps to <code>expenses.*</code> permission strings); <strong>Manage categories</strong> is <code>create</code> <strong>or</strong> <code>update</code> <strong>or</strong> <code>delete</code>; <code>PERMISSIONS.purchaseOrders.convert</code> reused for the PO action</td></tr><tr><td>Nav</td><td><strong>Purchasing</strong> sidebar group, after Purchase Orders — <code>permission: PERMISSIONS.expenses.view</code>, <code>module: &#39;expenses&#39;</code></td></tr><tr><td>Route</td><td><code>tenantRoutes.expenses = &#39;/expenses&#39;</code>, lazy-loaded in <code>App.tsx</code> behind <code>RequireAccess module=&quot;expenses&quot;</code></td></tr><tr><td>Notifications</td><td><code>src/notifications/modules/expenses.ts</code> — <code>expense.assigned</code> → <code>/expenses?expense={id}</code></td></tr><tr><td>PO detail sheet</td><td><code>purchase-order-detail-sheet.tsx</code> renders a <strong>Convert to expense</strong> button when <code>hasModule(&#39;expenses&#39;) &amp;&amp; hasPermission(&#39;purchase-orders.convert&#39;)</code> and the PO status is convertible and not already converted; shows a link to the created expense afterward</td></tr><tr><td>Playwright</td><td><code>e2e/pages/expenses.page.ts</code>, <code>e2e/tests/expenses/</code>, <code>npm run test:e2e:expenses</code></td></tr></tbody></table><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/Expense/ExpenseTest.php</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/Expense/ExpenseCategoryTest.php</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/PurchaseOrder/PurchaseOrderTest.php</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e:expenses</span></span></code></pre></div><h2 id="logging" tabindex="-1">Logging <a class="header-anchor" href="#logging" aria-label="Permalink to &quot;Logging&quot;">​</a></h2><ul><li>Spatie <code>LogsActivity</code> on <code>Expense</code> (log name <code>expenses</code>)</li><li>Domain <code>expense_activities</code> timeline</li><li><code>PlatformAuditService</code> via <code>ExpenseEventSubscriber</code></li></ul><h2 id="intentional-differences-from-purchase-orders" tabindex="-1">Intentional differences from Purchase Orders <a class="header-anchor" href="#intentional-differences-from-purchase-orders" aria-label="Permalink to &quot;Intentional differences from Purchase Orders&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Purchase Orders</th><th>Expenses</th></tr></thead><tbody><tr><td>Required <code>vendor_id</code>, hard-depends on Vendors</td><td>Optional <code>vendor_id</code> <strong>and</strong> <code>purchase_order_id</code>, no hard dependencies</td></tr><tr><td>Line-item child table, server-computed totals</td><td>Single <code>amount</code> + <code>tax_amount</code>, no computed total column</td></tr><tr><td>Statuses: <code>draft → sent → partially_received|received|cancelled</code></td><td>Statuses: <code>draft → submitted → approved|rejected</code>, <code>approved → paid</code>, <code>draft|submitted → cancelled</code></td></tr><tr><td><code>send</code> / <code>receive</code> / <code>cancel</code> actions, no convert</td><td><code>submit</code> / <code>approve</code> / <code>reject</code> / <code>pay</code> / <code>cancel</code> actions; <strong>is itself</strong> the target of a convert action</td></tr><tr><td>approve/reject/pay N/A</td><td><code>approve</code>/<code>reject</code>/<code>pay</code> are not assignee-scoped — any holder of the permission can act, modeling an approver role</td></tr></tbody></table><h2 id="deferred" tabindex="-1">Deferred <a class="header-anchor" href="#deferred" aria-label="Permalink to &quot;Deferred&quot;">​</a></h2><ul><li>Receipt attachments / file uploads</li><li>Reimbursement / payout tracking beyond the <code>paid</code> status</li><li>General ledger (GL) posting / accounting integration</li><li>Multi-line (itemized) expenses</li><li>Dashboard widgets for Expenses</li></ul>`,27)])])}const g=t(a,[["render",r]]);export{u as __pageData,g as default};
