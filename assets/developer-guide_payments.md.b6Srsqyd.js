import{_ as t,o,c as a,a3 as d}from"./chunks/framework.BTQ4eCVb.js";const u=JSON.parse('{"title":"Payments — Developer Guide","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Payments — Developer Guide | EloSync Docs"}],["meta",{"property":"og:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"property":"og:url","content":"https://docs.elosync.com/developer-guide/payments"}],["meta",{"property":"og:image","content":"https://docs.elosync.com/og-image.svg"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:title","content":"Payments — Developer Guide | EloSync Docs"}],["meta",{"name":"twitter:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"name":"twitter:image","content":"https://docs.elosync.com/og-image.svg"}],["link",{"rel":"canonical","href":"https://docs.elosync.com/developer-guide/payments"}]]},"headers":[],"relativePath":"developer-guide/payments.md","filePath":"developer-guide/payments.md","lastUpdated":1788477803000}'),s={name:"developer-guide/payments.md"};function n(i,e,c,r,l,p){return o(),a("div",null,[...e[0]||(e[0]=[d(`<h1 id="payments-—-developer-guide" tabindex="-1">Payments — Developer Guide <a class="header-anchor" href="#payments-—-developer-guide" aria-label="Permalink to &quot;Payments — Developer Guide&quot;">​</a></h1><p>Mirror of the <a href="/developer-guide/invoices">Invoices developer guide</a> (assignee scope, notes, domain timeline), with one structural difference: Payments declares a <strong>required</strong> <code>module_dependencies</code> row on Invoices — the first Phase 3 module to do so.</p><blockquote><p><strong>Naming:</strong> the backend model is <code>CustomerPayment</code> (table <code>customer_payments</code>) — Central&#39;s own platform-billing Payments ledger already exists for subscription payments tenants make <em>to</em> the platform. Frontend mirrors this with <code>customerPaymentService</code> / <code>PERMISSIONS.customerPayments</code> / <code>QUERY_KEYS.customerPayments</code>, distinct from the pre-existing <code>paymentService</code> / <code>PERMISSIONS.payments</code>.</p></blockquote><h2 id="backend-layout" tabindex="-1">Backend layout <a class="header-anchor" href="#backend-layout" aria-label="Permalink to &quot;Backend layout&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Models</td><td><code>app/Models/CustomerPayment.php</code>, <code>CustomerPaymentAllocation</code>, <code>CustomerPaymentNote</code>, <code>CustomerPaymentActivity</code></td></tr><tr><td>Enums</td><td><code>CustomerPaymentStatusEnum</code>, <code>CustomerPaymentMethodEnum</code>, <code>CustomerPaymentActivityTypeEnum</code></td></tr><tr><td>Service</td><td><code>app/Services/Tenant/CustomerPaymentService.php</code> (+ <code>ScopesToAssignee</code>)</td></tr><tr><td>Controller</td><td><code>app/Http/Controllers/Tenant/Api/V1/CustomerPaymentController.php</code></td></tr><tr><td>Requests</td><td><code>app/Http/Requests/Tenant/Api/V1/CustomerPayment/*</code></td></tr><tr><td>Resources</td><td><code>app/Http/Resources/Tenant/Api/V1/CustomerPayment/*</code></td></tr><tr><td>Policy</td><td><code>app/Policies/CustomerPaymentPolicy.php</code></td></tr><tr><td>Events</td><td><code>app/Events/CustomerPayment*.php</code></td></tr><tr><td>Subscriber</td><td><code>app/Listeners/CustomerPaymentEventSubscriber.php</code> (audit + assignment notification)</td></tr><tr><td>Notifications</td><td><code>app/Notifications/Tenant/CustomerPayment/CustomerPaymentAssignedNotification.php</code></td></tr><tr><td>Link rules</td><td><code>LinkableContact</code>, <code>LinkableCompany</code>, <code>EligiblePaymentAssignee</code> — <code>allocations.*.customer_invoice_id</code> is a plain tenant-scoped <code>Rule::exists()</code> on <code>CustomerInvoice</code></td></tr><tr><td>PDF / email</td><td><code>CustomerPaymentPdfService</code>, <code>CustomerPaymentEmailService</code>, <code>resources/views/payments/receipt.blade.php</code> — posted-only; mirrors invoice billing document mailer</td></tr><tr><td>Tests</td><td><code>tests/Feature/Tenant/CustomerPayment/CustomerPaymentTest.php</code>, <code>CustomerPaymentPdfTest.php</code>, <code>CustomerPaymentEmailTest.php</code></td></tr></tbody></table><h2 id="domain-notes" tabindex="-1">Domain notes <a class="header-anchor" href="#domain-notes" aria-label="Permalink to &quot;Domain notes&quot;">​</a></h2><ul><li><strong>Hard dependency</strong>: Payments declares a required <code>module_dependencies</code> row on Invoices (<code>database/migrations/2026_07_31_221006_add_payments_invoices_dependency.php</code>) — Marketplace install is blocked until Invoices is entitled. Contrast with Invoices itself, which has no hard dependency.</li><li>Status machine lives on <code>CustomerPaymentStatusEnum::allowedTransitions()</code> / <code>canTransitionTo()</code>: <code>draft → posted</code>, <code>posted → void</code>; <code>void</code> is terminal. There is no <code>sent</code>/<code>partial</code> equivalent — Payments only has these three states.</li><li><code>CustomerPaymentService::post()</code> / <code>void()</code> throw <code>ValidationException</code> (422, <code>status</code> field) for disallowed transitions.</li><li><code>post()</code> locks every allocation&#39;s invoice with <code>CustomerInvoice::query()-&gt;lockForUpdate()</code> (deliberately <strong>not</strong> <code>withTrashed()</code> — a soft-deleted invoice must not receive a payment) and validates all of them in a first pass before mutating anything, on <code>allocations</code> (naming the invoice number): invoice must exist, its status must be <code>sent</code>/<code>partial</code>, the allocation amount must not exceed its <code>balance_due</code> (0.01 float tolerance), and if both the payment and invoice have a <code>currency</code> set they must match. Only after every allocation passes does a second pass add each <code>amount</code> to its <code>CustomerInvoice::amount_paid</code> and call <code>CustomerInvoice::recalculateBalanceFromAmounts()</code> (recomputes <code>balance_due</code> and advances <code>sent → partial|paid</code>).</li><li><code>void()</code> locks each invoice <strong>with</strong> <code>withTrashed()</code> and does not re-check its status — this is intentional: a void is a ledger correction that must succeed even if the invoice was since fully paid by another payment or soft-deleted, otherwise <code>amount_paid</code> would permanently disagree with the payment record. It does the mirror-image subtraction and recalculation.</li><li>Content updates (<code>PUT</code>) — including replacing <code>allocations</code> — are <strong>draft-only</strong> via <code>CustomerPaymentService::isEditable()</code> (<code>status === draft</code>, same guard used for delete). Assignment remains available after posting via <code>POST …/assign</code>.</li><li>Allocations are a first-class child table (<code>customer_payment_allocations</code>), not embedded JSON — each row is <code>{ customer_payment_id, customer_invoice_id, amount }</code>, loaded with its <code>customerInvoice</code> ref for display.</li><li>Assignee scoping via <code>ScopesToAssignee</code> with <code>payments.assign</code>.</li><li><code>payments.force.delete</code> is not granted to any default role — owner/superadmin only.</li><li><code>contact_id</code> / <code>company_id</code> are optional and validated for module entitlement + assignee scope (<code>LinkableContact</code> / <code>LinkableCompany</code>), same pattern as Invoices.</li><li>Auto-numbering: <code>CustomerPaymentService::nextNumber()</code> reads the <code>payments_number_prefix</code> tenant setting (default <code>PAY-</code>), then zero-pads a running count to 5 digits — same pattern as Invoices&#39; <code>invoices_number_prefix</code>. Both prefixes are editable under <strong>Settings → General → Document number prefixes</strong>. <code>customer_payments</code> has a <code>unique(tenant_id, number)</code> DB index; <code>create()</code> retries up to 3 times via the shared <code>RetriesOnDuplicateNumber</code> trait on a duplicate-key collision.</li></ul><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>payments.view | create | update | delete | restore | force.delete | assign | post | void | send</span></span></code></pre></div><p>Routes use <code>module:payments</code> then <code>can:payments.*</code> / policies.</p><p>Catalog: slug <code>payments</code>, category <code>billing</code>, <code>is_default_included = false</code>, <code>is_billable = false</code>, <code>sort_order = 20</code>. Registered via <code>DefaultModuleRegistrar</code> migration (migrate-only), with a follow-up migration inserting the <code>module_dependencies</code> row on <code>invoices</code>.</p><h2 id="api-tenant" tabindex="-1">API (tenant) <a class="header-anchor" href="#api-tenant" aria-label="Permalink to &quot;API (tenant)&quot;">​</a></h2><p>Base: <code>/api/tenant/v1</code> — full reference <a href="/api/tenant-v1-payments">tenant-v1-payments.md</a>.</p><h2 id="frontend" tabindex="-1">Frontend <a class="header-anchor" href="#frontend" aria-label="Permalink to &quot;Frontend&quot;">​</a></h2><p>SPA mirrors <strong>Invoices</strong> (table + create/edit page, record page) under the existing AppLayout — do not invent a parallel shell.</p><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Page</td><td><code>src/pages/payments/</code> (<code>payments-page.tsx</code>, <code>payment-form-dialog.tsx</code>, <code>payment-detail-sheet.tsx</code>)</td></tr><tr><td>Detail sheet tabs</td><td>Overview, Allocations, Notes, Timeline — actions: assign, add note, post, void, edit (draft only), delete</td></tr><tr><td>Form dialog</td><td>Amount, currency, method, paid-at, reference, notes, contact/company/assignee pickers, and an allocations editor (<code>useFieldArray</code>). Allocation options show contact · company · invoice number — balance due; search matches name/company/number; selecting an invoice copies contact, company, and assignee onto the payment when present on the invoice.</td></tr><tr><td>Service</td><td><code>customerPaymentService</code> in <code>src/api/services.ts</code></td></tr><tr><td>Types</td><td><code>CustomerPayment*</code> in <code>src/types/api.ts</code> (kept distinct from the pre-existing Central <code>Payment*</code> types)</td></tr><tr><td>Query keys</td><td><code>QUERY_KEYS.customerPayments</code> / <code>customerPayment(id)</code> / <code>customerPaymentTimeline(id)</code> / <code>customerPaymentStats</code></td></tr><tr><td>Permissions</td><td><code>PERMISSIONS.customerPayments.*</code> (maps to <code>payments.*</code> permission strings)</td></tr><tr><td>Receipt actions</td><td><code>payment-view-page.tsx</code> — <strong>Download receipt</strong> + <strong>Email receipt</strong> (<code>payments.send</code>) when <code>status === posted</code>; shared <code>BillingDocumentEmailDialog</code> with <code>kind: &#39;payment&#39;</code></td></tr><tr><td>Nav</td><td><strong>Billing</strong> sidebar group, after Invoices — <code>permission: PERMISSIONS.customerPayments.view</code>, <code>module: &#39;payments&#39;</code>. Kept separate from the Central Billing nav.</td></tr><tr><td>Route</td><td><code>tenantRoutes.payments = &#39;/payments&#39;</code>, lazy-loaded in <code>App.tsx</code> behind <code>RequireAccess module=&quot;payments&quot;</code></td></tr><tr><td>Notifications</td><td><code>src/notifications/modules/payments.ts</code> — <code>customer_payment.assigned</code> → <code>/payments?payment={id}</code></td></tr><tr><td>Cross-link</td><td>Invoice record page shows a &quot;Related payments&quot; link to <code>/payments</code> when the Payments module + <code>payments.view</code> are both present</td></tr><tr><td>Playwright</td><td><code>e2e/pages/payments.page.ts</code>, <code>e2e/tests/payments/</code>, <code>npm run test:e2e:payments</code></td></tr></tbody></table><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/CustomerPayment/</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e:payments</span></span></code></pre></div><h2 id="logging" tabindex="-1">Logging <a class="header-anchor" href="#logging" aria-label="Permalink to &quot;Logging&quot;">​</a></h2><ul><li>Spatie <code>LogsActivity</code> on <code>CustomerPayment</code> (log name <code>customer_payments</code>)</li><li>Domain <code>customer_payment_activities</code> timeline</li><li><code>PlatformAuditService</code> via <code>CustomerPaymentEventSubscriber</code></li></ul>`,20)])])}const h=t(s,[["render",n]]);export{u as __pageData,h as default};
