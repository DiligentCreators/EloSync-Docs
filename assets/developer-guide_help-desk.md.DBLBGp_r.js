import{_ as o,o as t,c as d,a3 as s}from"./chunks/framework.BTQ4eCVb.js";const k=JSON.parse('{"title":"Help Desk — Developer Guide","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Help Desk — Developer Guide | EloSync Docs"}],["meta",{"property":"og:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"property":"og:url","content":"https://docs.elosync.com/developer-guide/help-desk"}],["meta",{"property":"og:image","content":"https://docs.elosync.com/og-image.svg"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:title","content":"Help Desk — Developer Guide | EloSync Docs"}],["meta",{"name":"twitter:description","content":"Official documentation for the EloSync SaaS Platform."}],["meta",{"name":"twitter:image","content":"https://docs.elosync.com/og-image.svg"}],["link",{"rel":"canonical","href":"https://docs.elosync.com/developer-guide/help-desk"}]]},"headers":[],"relativePath":"developer-guide/help-desk.md","filePath":"developer-guide/help-desk.md","lastUpdated":1788284805000}'),c={name:"developer-guide/help-desk.md"};function a(i,e,l,n,r,p){return t(),d("div",null,[...e[0]||(e[0]=[s(`<h1 id="help-desk-—-developer-guide" tabindex="-1">Help Desk — Developer Guide <a class="header-anchor" href="#help-desk-—-developer-guide" aria-label="Permalink to &quot;Help Desk — Developer Guide&quot;">​</a></h1><p>Simplified mirror of <a href="/developer-guide/expenses">Expenses</a> / <a href="/developer-guide/tasks">Tasks</a> (numbering, status machine, assignee scoping, notes, domain timeline) — <strong>no</strong> hard module dependencies. <code>contact_id</code> and <code>company_id</code> are both nullable soft links, validated only when the corresponding module is entitled.</p><h2 id="backend-layout" tabindex="-1">Backend layout <a class="header-anchor" href="#backend-layout" aria-label="Permalink to &quot;Backend layout&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Models</td><td><code>app/Models/HelpDeskTicket.php</code>, <code>HelpDeskCategory</code>, <code>HelpDeskSlaPolicy</code>, <code>HelpDeskMailbox</code>, <code>HelpDeskNote</code>, <code>HelpDeskActivity</code>, <code>HelpDeskTicketAttachment</code></td></tr><tr><td>Enums</td><td><code>HelpDeskStatusEnum</code>, <code>HelpDeskPriorityEnum</code>, <code>HelpDeskActivityTypeEnum</code> (includes <code>sla_applied</code>, <code>sla_response_met</code>, <code>sla_breached</code>)</td></tr><tr><td>Service</td><td><code>HelpDeskTicketService</code> (+ <code>ScopesToAssignee</code>, <code>RetriesOnDuplicateNumber</code>), <code>HelpDeskSlaClockService</code>, <code>HelpDeskSlaPolicyService</code>, <code>HelpDeskMailboxService</code>, <code>HelpDeskMailIngestService</code>, <code>HelpDeskCategoryService</code>, <code>HelpDeskCategorySeederService</code></td></tr><tr><td>Controller</td><td><code>HelpDeskTicketController</code>, <code>HelpDeskCategoryController</code>, <code>HelpDeskSlaPolicyController</code>, <code>HelpDeskMailboxController</code></td></tr><tr><td>Requests</td><td><code>app/Http/Requests/Tenant/Api/V1/HelpDesk/*</code>, <code>HelpDeskCategory/*</code>, <code>HelpDeskSlaPolicy/*</code>, <code>HelpDeskMailbox/*</code></td></tr><tr><td>Resources</td><td><code>app/Http/Resources/Tenant/Api/V1/HelpDesk/*</code>, <code>HelpDeskSlaPolicy/*</code>, <code>HelpDeskMailbox/*</code></td></tr><tr><td>Policy</td><td><code>HelpDeskTicketPolicy</code>, <code>HelpDeskCategoryPolicy</code>, <code>HelpDeskSlaPolicyPolicy</code>, <code>HelpDeskMailboxPolicy</code> (maps to <code>help-desk.*</code>)</td></tr><tr><td>Events</td><td><code>app/Events/HelpDeskTicket*.php</code>, <code>HelpDeskSlaBreached</code></td></tr><tr><td>Subscriber</td><td><code>app/Listeners/HelpDeskEventSubscriber.php</code> (audit + assignment/status/SLA notifications)</td></tr><tr><td>Notifications</td><td><code>HelpDeskAssignedNotification</code>, <code>HelpDeskStatusNotification</code>, <code>HelpDeskSlaBreachNotification</code></td></tr><tr><td>Automation</td><td>Wired triggers <code>help_desk.ticket_created</code>, <code>help_desk.ticket_status_changed</code>, <code>help_desk.sla_breached</code> via <code>AutomationTriggerRegistry</code> + <code>AutomationEventBridge</code></td></tr><tr><td>Commands / jobs</td><td><code>help-desk:scan-sla-breaches</code> (every 5 min); <code>help-desk:sync-mailboxes</code> (every minute) → <code>SyncHelpDeskMailboxJob</code> on queue <code>help-desk-ingest</code></td></tr><tr><td>Link rules</td><td><code>LinkableContact</code>, <code>LinkableCompany</code>, <code>LinkableKnowledgeBaseArticle</code> — optional, tenant-scoped, module-entitlement-checked</td></tr><tr><td>Pivot</td><td><code>help_desk_ticket_knowledge_base_article</code> — soft M2M (no <code>module_dependencies</code> row)</td></tr><tr><td>Tests</td><td><code>HelpDeskTicketTest</code>, <code>HelpDeskCategoryTest</code>, <code>HelpDeskKnowledgeBaseLinkTest</code>, <code>HelpDeskSlaTest</code>, <code>HelpDeskMailIngestTest</code></td></tr><tr><td>Migrations</td><td><code>2026_08_14_*</code> baseline … <code>2026_08_30_16000*</code> SLA (1.3.0) … <code>2026_08_30_11530*</code> mailboxes + email source (1.4.0)</td></tr></tbody></table><h2 id="domain-notes" tabindex="-1">Domain notes <a class="header-anchor" href="#domain-notes" aria-label="Permalink to &quot;Domain notes&quot;">​</a></h2><ul><li><strong>No hard module dependency</strong>: Help Desk has no <code>module_dependencies</code> row — installable standalone. <code>contact_id</code> / <code>company_id</code> are nullable columns.</li><li><strong>Tenant categories</strong>: <code>help_desk_categories</code> lookup (name, slug, <code>sort_order</code>, <code>is_active</code>, soft deletes). <code>help_desk_tickets.category_id</code> is a nullable FK. Category CRUD reuses <code>help-desk.view|create|update|delete|restore|force.delete</code> — no <code>help-desk-categories.*</code> family. <code>HelpDeskCategorySeederService::ensureDefaults()</code> lazily inserts General / Technical / Billing / Account / Other (slugs <code>general|technical|billing|account|other</code>) on first list/create; lazy seed does not write activity. Starter slugs are immutable on update. <strong>Other</strong> cannot be soft- or force-deleted (422). Listing does not restore deleted starters except a missing/trashed <strong>Other</strong>. Delete/forceDelete blocked while any tickets (including trashed, for force) still reference the category. Spatie log name <code>help-desk-categories</code>.</li><li>Status machine on <code>HelpDeskStatusEnum::allowedTransitions()</code> / <code>canTransitionTo()</code>: <code>open → in_progress|waiting|resolved|closed</code>, <code>in_progress → waiting|resolved|closed</code>, <code>waiting → in_progress|resolved|closed</code>, <code>resolved → closed|open</code>, <code>closed → open</code>. <code>HelpDeskTicketService::transitionStatus()</code> throws <code>ValidationException</code> (422, <code>status</code> field) for disallowed transitions.</li><li>Content updates (<code>PUT</code>) blocked when <code>status === closed</code> via <code>HelpDeskTicket::isEditable()</code>. Assignment after submit uses <code>POST …/assign</code>.</li><li><code>close()</code> / <code>reopen()</code> are assignee-scoped in <code>HelpDeskTicketPolicy</code> (same as <code>view</code> / <code>update</code>) unless the actor has <code>help-desk.assign</code> or is superadmin.</li><li><code>contact_id</code> / <code>company_id</code> validated via <code>LinkableContact</code> / <code>LinkableCompany</code> — null always passes; non-null requires module entitlement, tenant scope, and assignee rules when applicable.</li><li><code>knowledge_base_article_ids</code> on create/update and <code>PUT …/articles</code> sync via <code>HelpDeskTicketService::syncKnowledgeBaseArticles()</code> — requires <code>module:knowledge-base</code>; <code>LinkableKnowledgeBaseArticle</code> allows published for view-only actors or any visible article when actor has <code>knowledge-base.update</code>. Records <code>articles_synced</code> on the domain timeline when the set changes.</li><li><code>help-desk.force.delete</code> is not granted to any default role — owner/superadmin only.</li><li>Auto-numbering: <code>HelpDeskTicketService::nextNumber()</code> reads <code>help_desk_number_prefix</code> tenant setting (default <code>HD-</code>), zero-pads running count to 5 digits. Exposed via <code>PUT /settings</code> (<code>UpdateTenantSettingsRequest</code>). <code>unique(tenant_id, number)</code> DB index; <code>create()</code> retries up to 3 times via <code>RetriesOnDuplicateNumber</code>.</li><li>Overdue scope: open statuses (<code>open</code>, <code>in_progress</code>, <code>waiting</code>) with <code>due_at &lt; UtcInstant::now()</code> — aligns with workspace timezone KPI fixes elsewhere.</li><li><strong>SLA clocks</strong> (<code>HelpDeskSlaClockService</code>): most-specific active policy (category+priority &gt; priority &gt; category &gt; default); <code>UtcDateTime</code> columns; first response on first staff note or leave-<code>open</code> transition; <code>help-desk:scan-sla-breaches</code> marks response/resolve breaches via <code>UtcInstant</code>.</li><li><strong>Email intake</strong>: tenant-scoped <code>help_desk_mailboxes</code> (encrypted IMAP password); ingest reuses <code>MailboxClient</code> / <code>MailboxConnection</code> (not personal <code>EmailAccount</code>); replies matched by ticket number prefix; idempotent <code>source_message_id</code>.</li><li>Dashboard widget: <code>DashboardWidgetService</code> registers <code>help_desk_my_open</code> gated by <code>module:help-desk</code> + <code>help-desk.view</code>.</li></ul><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>help-desk.view | create | update | delete | restore | force.delete | assign | close | reopen</span></span></code></pre></div><p>Routes use <code>module:help-desk</code> then <code>can:help-desk.*</code> / policies. SLA policy and mailbox CRUD reuse the same permissions (categories pattern).</p><p>Catalog: slug <code>help-desk</code>, category <code>operations</code>, <code>is_default_included = false</code>, <code>is_billable = false</code>, <code>sort_order = 10</code>, version <strong>1.7.0</strong>. Registered via <code>DefaultModuleRegistrar</code> migration (migrate-only) — <strong>no</strong> <code>module_dependencies</code> row.</p><h2 id="communication-templates-soft" tabindex="-1">Communication Templates (soft) <a class="header-anchor" href="#communication-templates-soft" aria-label="Permalink to &quot;Communication Templates (soft)&quot;">​</a></h2><p><code>HelpDeskPlaceholderProvider</code> registers context <code>help-desk</code> on <code>PlaceholderRegistry</code> (see <a href="/developer-guide/communication-templates">Communication Templates</a>). Ticket placeholders include <code>number</code>, <code>subject</code>, <code>status</code>, <code>priority</code>, <code>category_name</code>, <code>name</code> / <code>email</code> / <code>phone</code> (from linked contact), <code>company_name</code>, <code>assignee_name</code>, plus shared agent/workspace/system tokens. WhatsApp <code>phone</code> resolves from the soft-linked contact — tickets without a contact phone cannot render WhatsApp extras.</p><p>SPA: ticket view soft-gates the shared <code>WhatsAppTemplatePickerDialog</code> when <code>module:communication-templates</code> + <code>communication-templates.use</code> and the linked contact has a phone. <strong>No</strong> <code>module_dependencies</code> row between Help Desk and Communication Templates.</p><h2 id="api-tenant" tabindex="-1">API (tenant) <a class="header-anchor" href="#api-tenant" aria-label="Permalink to &quot;API (tenant)&quot;">​</a></h2><p>Base: <code>/api/tenant/v1</code> — full reference <a href="/api/tenant-v1-help-desk">tenant-v1-help-desk.md</a>.</p><h2 id="frontend" tabindex="-1">Frontend <a class="header-anchor" href="#frontend" aria-label="Permalink to &quot;Frontend&quot;">​</a></h2><p>SPA mirrors <strong>Expenses</strong> (dedicated create/view/edit pages, no create/edit page or record page) under AppLayout.</p><table tabindex="0"><thead><tr><th>Piece</th><th>Path</th></tr></thead><tbody><tr><td>Page</td><td><code>src/pages/help-desk/</code> (<code>help-desk-page.tsx</code>, <code>help-desk-form.tsx</code>, <code>help-desk-form-page.tsx</code>, <code>help-desk-view-page.tsx</code>, <code>help-desk-categories-dialog.tsx</code>, <code>help-desk-sla-policies-dialog.tsx</code>, <code>help-desk-mailboxes-dialog.tsx</code>)</td></tr><tr><td>View page</td><td>Details (category, priority, status, due date, SLA clocks, source, assignee, related contact/company + soft-gated WhatsApp template picker, related KB articles), notes with <code>@mentions</code>, timeline — actions: assign, add note, status transitions, close, reopen, edit (non-closed), delete</td></tr><tr><td>Form page</td><td>Subject, description, category picker, priority, due date, conditional contact/company pickers, and <strong>Knowledge base articles</strong> multi-select when <code>hasModule(&#39;knowledge-base&#39;)</code> + <code>knowledge-base.view</code></td></tr><tr><td>Service</td><td><code>helpDeskService</code> + <code>helpDeskCategoryService</code> + <code>helpDeskSlaPolicyService</code> + <code>helpDeskMailboxService</code> in <code>src/api/services.ts</code></td></tr><tr><td>Types</td><td><code>HelpDesk*</code> in <code>src/types/api.ts</code></td></tr><tr><td>Query keys</td><td><code>QUERY_KEYS.helpDeskTickets</code> / <code>helpDeskTicket(id)</code> / <code>helpDeskTicketTimeline(id)</code> / <code>helpDeskStats</code> / <code>helpDeskCategories</code> / <code>helpDeskSlaPolicies</code> / <code>helpDeskMailboxes</code></td></tr><tr><td>Permissions</td><td><code>PERMISSIONS.helpDesk.*</code></td></tr><tr><td>Nav</td><td><strong>Operations</strong> sidebar group — <code>permission: PERMISSIONS.helpDesk.view</code>, <code>module: &#39;help-desk&#39;</code></td></tr><tr><td>Route</td><td><code>tenantRoutes.helpDesk = &#39;/help-desk&#39;</code>, lazy-loaded in <code>App.tsx</code> behind <code>RequireAccess module=&quot;help-desk&quot;</code></td></tr><tr><td>Dashboard</td><td><code>tenant-dashboard-widgets.tsx</code> — <code>help_desk_my_open</code> widget</td></tr><tr><td>Notifications</td><td><code>src/notifications/modules/help-desk.ts</code> — assigned/closed/reopened/due/overdue/SLA breach types (deep link <code>/help-desk/:id</code>)</td></tr><tr><td>Playwright</td><td><code>e2e/pages/help-desk.page.ts</code>, <code>e2e/tests/help-desk/</code>, <code>npm run test:e2e:help-desk</code></td></tr></tbody></table><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Tenant/HelpDesk/</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e:help-desk</span></span></code></pre></div><h2 id="logging" tabindex="-1">Logging <a class="header-anchor" href="#logging" aria-label="Permalink to &quot;Logging&quot;">​</a></h2><ul><li>Spatie <code>LogsActivity</code> on <code>HelpDeskTicket</code> (log name <code>help-desk</code>)</li><li>Domain <code>help_desk_activities</code> timeline</li><li><code>PlatformAuditService</code> via <code>HelpDeskEventSubscriber</code></li></ul><h2 id="distinct-from-central-feedback" tabindex="-1">Distinct from Central Feedback <a class="header-anchor" href="#distinct-from-central-feedback" aria-label="Permalink to &quot;Distinct from Central Feedback&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Central Feedback</th><th>Help Desk</th></tr></thead><tbody><tr><td>Platform concern — no <code>module:*</code> gate</td><td>Licensed <code>module:help-desk</code></td></tr><tr><td>Tenant submit → Central triage</td><td>Tenant-scoped internal queue</td></tr><tr><td><code>feedback.*</code> permissions (Central)</td><td><code>help-desk.*</code> permissions (Tenant)</td></tr><tr><td>Product bug/feature intake</td><td>Workspace support / ops tickets</td></tr></tbody></table><p>See <a href="/developer-guide/central-feedback-system">Central Feedback System</a>.</p><h2 id="deferred" tabindex="-1">Deferred <a class="header-anchor" href="#deferred" aria-label="Permalink to &quot;Deferred&quot;">​</a></h2><ul><li>Customer portal, chat/social intake, Kanban</li></ul>`,27)])])}const u=o(c,[["render",a]]);export{k as __pageData,u as default};
